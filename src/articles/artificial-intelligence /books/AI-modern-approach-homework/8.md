## 将以下自然语句翻译成一阶逻辑公式（定义必要的谓词、函数、常量）：
- “所有数学家都是逻辑学家。”
- “有些教授喜欢所有的学生。”
- “每个城市都有一个大学。”
- “张三的母亲是李四。” （可以使用 MotherOf(x,y) 表示 x 是 y 的母亲）

## 解释量词作用域、自由变量、绑定变量。判断以下公式是否合法：
- (a) ∀x P(x) → Q(x)
- (b) ∃x (P(x) ∧ ∀x Q(x))
- (c) ∀x ∃y R(x,y) → ∃y R(y,x)

并指出其中变量的出现。

## 给定知识库：
•	∀x (Cat(x) → Mammal(x))
•	Cat(Mittens)
•	∀x (Mammal(x) → Animal(x))
使用前向链接推导出 Animal(Mittens)，写出每一步的合一替换。

## 计算以下表达式对的最一般合一器（如果存在）：
- P(f(x, g(y)), h(z)) 与 P(f(a, g(b)), h(c))
- Q(x, y, f(x, a)) 与 Q(g(z), z, f(g(z), a))
- R(x, f(x, b)) 与 R(g(y), f(g(y), y)) （是否存在合一？若存在，给出MGU）

## 用反向链接证明 Animal(Mittens) 是否可以从题目3的知识库中推出。画出反向链接的与或树，并标出合一步骤。

## 将以下一阶逻辑公式转换为CNF（合取范式），为归结做准备（注意先消去蕴含、移动否定符号、Skolem化）：
`∀x ∃y (P(x) → (Q(y) ∧ ¬R(x,y))) ∧ ¬∀x S(x)`

## 家族关系建模
定义谓词 Parent(x,y), Male(x), Female(x)。

写出定义“父亲”、“母亲”、“兄弟”、“姐妹”、“祖先”的一阶逻辑公理。

从以下事实：Parent(王强, 王丽), Parent(王强, 王明), Male(王强), Female(王丽), Male(王明)，使用前向链接推导出“王丽和王明是兄妹”。写出推导过程（合一及规则触发）。

## 归纳证明
用自然数归纳公理：

∀P [ (P(0) ∧ ∀n (P(n) → P(s(n))) ) → ∀n P(n) ]

证明：∀n (sum(0,n) = n) 和 ∀n ∀m (sum(s(n), m) = s(sum(n, m))) 可推导出 ∀n sum(n, 0) = n。
（仅需写出归纳步骤，无需完全形式化）

## 学术会议论文审稿系统
设计一个小型一阶逻辑知识库，用于判断一篇论文是否被接收。规则包括：

如果论文有创新性且没有重大错误，则接收。

如果评审人总数至少为3，且至少2人推荐接收，则接收。

如果出现抄袭，则拒收。
定义所需的谓词和常量（例如 HasInnovation(p), HasPlagiarism(p), ReviewerRecommend(r,p) 等）。
给定具体事实（如某篇论文有创新性，无抄袭，3个评审人中2人推荐），写出查询公理，并用一阶逻辑前向链接或反向链接推理该论文是否接收。

## 比较命题化与直接合一推理
假设你有一个包含量词的大知识库（例如关于所有自然数的事实）。如果使用命题化（将每个实例像 Nat(0), Nat(1) ... 无限展开），显然不可行。说明合一算法（如在前向链接中）如何无需展开所有实例就能匹配规则。举一个简单例子：规则 ∀x (Human(x) → Mortal(x))，事实 Human(Socrates)。解释合一算法如何将变量 x 绑定为 Socrates。