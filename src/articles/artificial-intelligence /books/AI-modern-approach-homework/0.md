# CS188 教材自测题（按小节顺序）

以下题目覆盖教材第1章到第10章每个子节的核心内容。请先阅读对应小节，再尝试回答。所有题目均为简答或计算题，答案及解释在最后单独列出。

---

## 第1章 Search

### 1.1 Agents
1. 什么是理性智能体（rational agent）？理性智能体旨在按照优效用获得最优目标的智能体。
2. 反射型智能体（reflex agent）与规划型智能体（planning agent）的核心区别是什么？反射型智能体是根据当前感知做出反馈，更像膝盖反射。
3. PEAS 代表哪四个要素？请举例说明。P：性能，最终效果的评价、E：环境，智能体所处的环境、A：动作，智能体可采取的动作、S：感知，智能体接收到的信息。
4. 环境可以分为部分可观测与完全可观测。请各举一例。部分可观测：Cursor编码智能体，它智能观测到代码，并不能观测代码执行或代码的设计规范意图等，完全可观测：除草机器人，在 2*2 菜畦中，机器人可知道当前菜畦是否有草。
5. 随机性环境（stochastic）与确定性环境（deterministic）的区别是什么？同一动作可能产生不同结果（例如投掷飞镖时突然来了一阵风），同一动作结果相同（例如真空环境中投掷飞镖）。
6. 多智能体环境为什么可能需要随机化动作？避免被对手利用（避免对手预测）。
7. 静态环境与动态环境的区别是什么？智能体思考过程中环境是否变化。
8. 什么是环境“已知物理”（known physics）？这对智能体有何帮助？根据公式能推到处环境的确定状态，这可以帮助智能体增强信念，同一个动作可以产生同样的结果。
9. 如果一个智能体在完全可观测、确定性、静态、单智能体环境中，它是否还需要传感器？为什么？需要，静态环境是思考过程中不变，采取行动后，世界还是会变化，需要传感器重新回传信息。
10. 为什么规划智能体需要“世界模型”？世界模型用于建立智能体的信念，通过信念来规划动作。尤其是在非完全可观测环境中。

### 1.2 State Spaces and Search Problems
1. 一个搜索问题由哪六个要素组成？
2. 世界状态（world state）与搜索状态（search state）有何不同？
3. 在 Pacman 的“吃所有豆子”问题中，为什么搜索状态通常比世界状态更紧凑？
4. 解释“状态空间大小”的计算方法。如果 Pacman 有 120 个位置、4 个方向、2 个幽灵（各 12 个位置）、30 个豆子（每个存在/吃掉），总状态数是多少？（表达式即可）
5. 什么是状态空间图（state space graph）？它与搜索树（search tree）的主要区别是什么？
6. 为什么状态空间图通常不能完整存储在内存中？
7. 搜索树中为什么同一个状态可能出现多次？
8. 搜索树的大小与状态空间图的大小关系如何？
9. 在搜索问题中，如何动态生成子节点而不事先存储整个图？
10. 什么是“计划”（plan）？它在搜索树中如何表示？

### 1.3 Uninformed Search
1. 树搜索（tree search）算法的通用框架步骤是什么？
2. 深度优先搜索（DFS）使用什么数据结构作为 frontier？为什么？
3. DFS 是否完备？是否最优？为什么？
4. 深度优先搜索的时间复杂度和空间复杂度分别是多少（用分支因子 b 和最大深度 m 表示）？
5. 广度优先搜索（BFS）使用什么数据结构作为 frontier？为什么？
6. BFS 在什么条件下是最优的？
7. BFS 的时间复杂度和空间复杂度分别是多少（用分支因子 b 和最优解深度 s 表示）？
8. 一致代价搜索（UCS）使用什么数据结构作为 frontier？优先级依据是什么？
9. UCS 的完备性和最优性条件是什么？如何处理负边权？
10. 设最优解代价为 C*，最小边权为 ε，UCS 的时间复杂度是多少？

### 1.4 Informed Search
1. 什么是启发式函数（heuristic）？它通常如何得到？
2. 曼哈顿距离（Manhattan distance）如何计算？在 Pacman 中它对应什么松弛问题？
3. 贪婪搜索（greedy search）选择节点的依据是什么？它是否完备或最优？
4. A* 搜索的评估函数 f(n) 如何定义？其含义是什么？
5. 什么是可采纳性（admissibility）？写出数学表达式。
6. 证明：如果启发式是可采纳的，则 A* 树搜索保证最优性。（简述关键步骤）
7. 为什么贪心启发式 h(n)=1-g(n) 会导致 A* 退化为 BFS？
8. 什么是图搜索（graph search）？它比树搜索多了什么？
9. 在图搜索中使用 A* 时，为什么仅记录已访问状态可能破坏最优性？举例说明。
10. 如何通过“最大启发式”来获得更优的启发式函数？

### 1.5 Local Search
1. 局部搜索与前面讲的搜索问题最大的不同是什么？
2. 什么是“状态空间地形图”？全局最大值、局部最大值、平原、肩部分别指什么？
3. 爬山法（hill-climbing）的缺点有哪些？如何缓解？
4. 随机爬山法（stochastic hill-climbing）与普通爬山法有何区别？
5. 模拟退火（simulated annealing）如何平衡探索与利用？温度参数的作用是什么？
6. 模拟退火中接受较差解的概率公式是什么？为什么使用指数形式？
7. 局部束搜索（local beam search）与运行 k 次独立爬山法有何不同？
8. 遗传算法中的“个体”、“种群”、“适应度函数”如何定义？以 8 皇后为例说明。
9. 遗传算法中的交叉（crossover）和变异（mutation）分别起什么作用？
10. 为什么局部搜索算法在解决 N 皇后问题时往往非常快？

---

## 第2章 CSPs

### 2.1 Constraint Satisfaction Problems
1. CSP 的三个组成部分是什么？
2. 如何将 N 皇后问题建模为 CSP？写出变量、值域、至少两类约束。
3. CSP 与一般搜索问题的主要区别是什么？（提示：识别问题 vs 规划问题）
4. 为什么说 CSP 是 NP-hard 的？完全赋值数量是多少（用变量数 N 和域大小 d 表示）？
5. 在 CSP 中，什么是部分赋值（partial assignment）？什么是状态的后继函数？
6. 约束图（constraint graph）的节点和边分别代表什么？
7. 三元约束（higher-order constraint）在约束图上如何表示？举例。
8. 地图着色问题中，约束图的边是如何确定的？
9. 为什么约束图的结构对求解 CSP 很重要？
10. 写出澳大利亚地图着色问题中至少三个二元约束。

### 2.2 Solving Constraint Satisfaction Problems
1. 什么是回溯搜索（backtracking search）？它与深度优先搜索有何不同？
2. 回溯搜索为什么能比普通 DFS 减少搜索量？
3. 回溯搜索中变量赋值的顺序固定，为什么不会丢失解？（提示：交换性）
4. 如果当前变量没有可用值（所有值都与已赋值冲突），回溯搜索会做什么？
5. 画出简单地图着色（三个区域两两相邻）时，DFS 和回溯搜索的搜索树差异。
6. 回溯搜索的最坏情况时间复杂度是多少？
7. 为什么要“提前检查”变量顺序？MRV 和 LCV 分别是什么？
8. 如果回溯搜索遇到一个变量域为空，但之前的选择导致，它会如何处理？
9. 回溯搜索是否总是能找到解（如果存在）？为什么？
10. 说明回溯搜索在使用“最少约束值”启发式时可能带来的代价。

### 2.3 Filtering
1. 什么是前向检查（forward checking）？它在何时进行？
2. 前向检查与完整弧一致性（arc consistency）的区别是什么？
3. 解释 AC-3 算法的基本步骤：队列、弧、域缩减、传播。
4. AC-3 的时间复杂度是多少？（用弧数 e 和最大域大小 d 表示）
5. 在澳大利亚地图着色中，如果 WA=红，Q=绿，使用弧一致性后 SA 的域可能如何变化？
6. 为什么前向检查比弧一致性更快？为什么有时仍要使用弧一致性？
7. 什么是 k-一致性？2-一致性等价于什么？
8. 强 k-一致性意味着什么？
9. 在 CSP 中，一个变量经过 AC-3 后域变为空，这意味着什么？
10. 写出 AC-3 中 REMOVE-INCONSISTENT-VALUES 的伪代码描述。

### 2.4 Ordering
1. MRV（最小剩余值）启发式的直观理由是什么？
2. 假设有三个变量：X 域 {1,2}，Y 域 {1}，Z 域 {1,2,3}，选择哪个变量先赋值（MRV）？
3. LCV（最少约束值）启发式如何选择值？它需要额外计算什么？
4. 树形 CSP（tree-structured CSP）可以在什么时间复杂度内求解？
5. 树形 CSP 算法分哪两步？先做什么后做什么？
6. 什么是割集（cutset）？割集条件（cutset conditioning）如何扩展树形算法？
7. 假设一个 CSP 的约束图去掉 3 个节点后成为树，那么总体时间复杂度是多少（用变量数 n，域大小 d，割集大小 c）？
8. 为什么 MRV 在某些问题中效果显著，在另一些中几乎不影响？
9. 能否同时使用 MRV 和 LCV？如果可以，顺序如何？
10. 如果约束图是一棵树，使用树形算法是否还需要回溯？为什么？

### 2.5 Local Search
1. 局部搜索在 CSP 中如何初始化状态？
2. 什么是最小冲突启发式（min-conflicts heuristic）？如何应用于 N 皇后？
3. 给定一个 4 皇后部分冲突状态，如何用最小冲突选择下一次移动？
4. 最小冲突法为什么能在 N 皇后上很快找到解？
5. 局部搜索求解 CSP 有什么缺点？
6. 什么是“临界比例”（critical ratio）？对局部搜索性能有何影响？
7. 如何在 CSP 中使用爬山法？它与 min-conflicts 有何异同？
8. 模拟退火在 CSP 中如何设置接受的较差移动？
9. 遗传算法应用于 CSP 时，如何设计适应度函数？
10. 为什么说局部搜索对于随机生成的 CSP 往往成功率很高？

---

## 第3章 Games

### 3.1 Games
1. 什么是零和博弈（zero-sum game）？举例。
2. 什么是“已解游戏”（solved game）？举例。
3. 对抗性搜索（adversarial search）返回的是计划（plan）还是策略（policy）？为什么？
4. 标准游戏形式化包含哪六个要素？
5. 在博弈树中，什么是“节点控制者”？
6. 为什么博弈中智能体不能直接使用标准搜索算法（如 UCS）？
7. 什么是“策略”（policy）？与“计划”（plan）有何不同？
8. 为什么 Deep Blue 能在象棋中击败人类，而 Go 的突破来得更晚？
9. 如果博弈中存在不确定性（如掷骰子），它属于哪一类博弈？
10. 什么是“洗牌算法”？在博弈中起到什么作用？

### 3.2 Minimax
1. Minimax 算法对对手的行为做了何种假设？
2. 一个非终止状态的值如何递归定义？
3. 在包含对手的博弈树中，怎么区分最大化节点和最小化节点？
4. 写出 Minimax 状态值的更新公式（最大化层、最小化层、终止层）。
5. 给定一个深度为 2 的博弈树，叶子节点值分别为 [3,12,8] 和 [2,4,6] 和 [14,5,2]，根节点是最大化，计算根节点值。
6. 什么是 α-β 剪枝？它的核心思想是什么？
7. 在最佳情况下，α-β 剪枝能将 Minimax 的时间复杂度从 O(b^m) 降至多少？
8. 为什么 α-β 剪枝的效果依赖于节点考察顺序？
9. 什么是评估函数（evaluation function）？为什么需要它？
10. 设计一个简单的国际象棋评估函数，列出至少三个特征及其权重思路。

### 3.3 Expectimax
1. Expectimax 与 Minimax 的主要区别是什么？
2. 机会节点（chance node）如何计算期望值？
3. 假设一个机会节点有三个子节点，值分别为 5, 10, 15，且概率相等，该节点的期望值是多少？
4. 为什么 Minimax 是 Expectimax 的特例？
5. 在 Expectimax 中，概率分布通常来自哪里？（至少两个来源）
6. 与 Minimax 相比，Expectimax 在计算上能否进行 α-β 剪枝？为什么？
7. 考虑一个博弈：我方先动，然后对手以 0.6 概率选择左、0.4 概率选择右，然后到达终止值。写出期望值计算式。
8. 如果对手的行为不是最优而只是随机，Minimax 可能过于悲观，请解释。
9. 如何在一个博弈树中混合使用最大节点、最小节点和机会节点？举例。
10. 在 Pacman 中如果有多个幽灵，每个幽灵依次移动，如何用 Expectimax 建模？

### 3.4 General Games
1. 什么是多智能体效用（multi-agent utility）？如何表示？
2. 在非零和博弈中，每个智能体节点的目标是什么？
3. 给定一个三智能体博弈树，叶子效用元组为 (5,2,5)、(3,1,6)、(4,4,4)，第一层红（最大化红色分量），第二层绿（最大化绿色分量），第三层蓝（最大化蓝色分量），求根节点值。
4. 多智能体博弈中为什么可能自然出现合作？
5. 如果两个智能体有完全一致的效用函数，博弈会变成什么形式？
6. 帕累托最优（Pareto optimal）与纳什均衡在博弈中分别指什么？本课程不要求详细证明，能简要说明即可。
7. 在一般博弈中，为什么不能简单地使用 Minimax 的“最大化-最小化”交替？
8. 如果三个智能体各自独立最大化自己的效用，但其中一个智能体可以发送承诺，这会影响结果吗？
9. 举例说明一个非零和博弈的实际情况（比如市场共享）。
10. 多智能体博弈中的“混合策略”是什么？与零和博弈中的随机化有何关系？

### 3.5 Monte Carlo Tree Search
1. MCTS 的两个核心思想是什么？
2. 什么是“rollout”？为什么要随机模拟多次？
3. 在 MCTS 中，如何平衡探索（exploration）和利用（exploitation）？
4. 写出 UCB1 公式，并解释各项含义。
5. UCB1 中参数 C 的作用是什么？C 增大时更倾向于探索还是利用？
6. MCTS 的 UCT 算法的三个主要步骤是什么？
7. 为什么 MCTS 在处理巨大分支因子（如围棋）时比 Minimax 更有效？
8. 在 MCTS 中，是否需要一个评估函数？与 Minimax 的评估函数有何不同？
9. 当模拟次数趋于无穷时，UCT 的行为会趋近于什么？
10. MCTS 相比于 α-β 搜索的优缺点各一个。

---

## 第4章 MDPs

### 4.1 Markov Decision Processes
1. MDP 由哪些要素定义？（列出至少 5 个）
2. 什么是转移函数 T(s,a,s')？它的输出是什么？
3. 什么是奖励函数 R(s,a,s')？MDP 中总回报如何计算？
4. 在 MDP 中，Q-状态 (s,a) 是什么？为什么是“状态-动作对”？
5. 折扣因子 γ 的作用是什么？γ=0 和 γ=1 分别代表什么极端情况？
6. 为什么需要折扣因子？如果不使用折扣，可能导致什么问题？
7. 给定一个简单 MDP：两个状态，一个动作，奖励固定为+1，γ=0.9，求从初始状态开始无限步的累积折扣奖励。
8. 什么是“吸收状态”（absorbing state）？通常其奖励如何设置？
9. 在 MDP 中，智能体采取动作后是否一定能到达确定的下一个状态？为什么？
10. 请将一个小型 MDP（2-3 个状态）画出状态转移图，包括动作、概率和奖励。

### 4.2 Solving Markov Decision Processes
1. 求解 MDP 的目标是什么？写出符号定义。
2. 最优策略 π* 是一个从什么到什么的映射？
3. 什么是策略（policy）？为什么它定义了一个反射型智能体？
4. 贝尔曼方程（Bellman equation）的基本形式是什么？用 V*(s) 表示。
5. 最优 Q-值 Q*(s,a) 如何定义？它与 V*(s) 的关系是什么？
6. 贝尔曼方程为什么被称为“动态规划”方程？
7. 在确定性的 MDP 中，贝尔曼方程会简化成什么形式？
8. 如果已知 Q*(s,a)，如何提取最优策略？
9. 给定一个 MDP，如果 V*(s) 满足贝尔曼方程，能否保证它是真正的最优值？
10. 为什么策略是状态到动作的函数，而不是动作序列？

### 4.3 Value Iteration
1. 值迭代（value iteration）中 V0(s) 通常是初始化为多少？为什么？
2. 值迭代的更新公式是什么？（写出 V_{k+1}(s) 的表达式）
3. 值迭代的收敛条件是什么？
4. 什么是贝尔曼算子（Bellman operator）？为什么说它是 γ-收缩？
5. 值迭代的收缩性质保证了什么？
6. 在值迭代中，从 V2 到 V3 需要什么信息？
7. 策略提取（policy extraction）如何从 V* 获得 π*？
8. 如果存储的是 Q*，提取策略需要多少计算？
9. 值迭代的每次迭代的时间复杂度大致是多少（用 |S|, |A|, |S'| 表示）？
10. 给出一个简单 MDP，运行两轮值迭代的例子（初始 V0=0，γ=0.9）。

### 4.4 Policy Iteration
1. 策略迭代与值迭代的主要区别是什么？
2. 策略迭代的初始策略可以任意设定吗？越接近最优策略会怎样？
3. 策略评估（policy evaluation）要解决什么问题？写出方程。
4. 策略评估可以通过求解线性方程组完成。为什么这比值迭代快？
5. 策略改善（policy improvement）使用什么方法更新策略？
6. 什么条件下策略迭代收敛？
7. 在策略评估中，如果直接求解方程组代价太大，可以采用什么近似方法？
8. 为什么策略迭代通常比值迭代收敛更快？
9. 以赛车 MDP 为例（见教材），初始策略为“always slow”，写出策略评估的方程组。
10. 策略迭代的每一轮复杂度与值迭代相比如何？

---

## 第5章 RL

### 5.1 Reinforcement Learning
1. 离线规划（offline planning）与在线规划（online planning）的核心区别是什么？
2. 在强化学习中，“探索”（exploration）和“利用”（exploitation）分别指什么？
3. 什么是样本（sample）？什么是情节（episode）？
4. 模型基（model-based）与模型自由（model-free）学习的主要区别是什么？
5. 为什么在 RL 中，智能体开始时不知道转移函数和奖励函数？
6. 如果智能体只知道部分 MDP 模型，它还能进行规划吗？
7. 在 RL 中，一个完整的“情节”是指什么？
8. 为什么 RL 有时被称为“试错学习”？
9. 试想一个智能体在 Gridworld 中移动，每次得到一个样本 (s,a,s',r)，这包含什么信息？
10. 你如何衡量一个 RL 算法的性能？什么是“遗憾”（regret）？

### 5.2 Model-Based Learning
1. 模型基学习中如何估计转移函数 \hat{T}(s,a,s')？具体步骤。
2. 假设从 (s,a) 出发，观察到 3 次到达 s1，2 次到达 s2，估计 \hat{T}(s,a,s1) 和 \hat{T}(s,a,s2) 分别是多少？
3. 在模型基学习中，奖励函数 \hat{R} 如何估计？
4. 一旦获得了 \hat{T} 和 \hat{R}，如何得到用于利用（exploitation）的策略？
5. 模型基学习的缺点是什么？
6. 从 4 个情节中收集到表格样本，计数为： (A,exit,x):1, (B,east,C):2, ... 如何规范化得到概率？
7. 为什么需要多次情节才能获得可靠的模型？
8. 在模型基学习中，是否可以同时进行探索和利用？如何安排？
9. 模型基学习是否收敛到真实 MDP 解？大数定律如何保证？
10. 比较模型基学习的时间复杂度和模型自由学习的时间复杂度。

### 5.3 Model-Free Learning
1. 什么是直接评价（direct evaluation）？它如何估计 V^π(s)？
2. 直接评价的缺点是什么？（至少两个）
3. 时序差分学习（TD learning）的更新公式是什么？
4. TD 学习中学习率 α 的作用是什么？如何调整？
5. 推导 TD 更新公式如何实现指数衰减权重的历史样本平均。
6. Q-学习的更新公式是什么？它是 off-policy 还是 on-policy？为什么？
7. 什么是 off-policy 学习？举例说明。
8. 近似 Q-学习（approximate Q-learning）如何利用特征向量？更新规则是什么？
9. 为什么近似 Q-学习比表格 Q-学习更能泛化？
10. 在 Pacman 中，设计哪些特征可以用于近似 Q-学习？

### 5.4 Exploration and Exploitation
1. ε-贪婪策略如何工作？ε 接近 1 时智能体行为如何？
2. ε-贪婪策略的缺点是什么？如何改进？
3. 什么是探索函数（exploration function）？写出一种常见形式 f(s,a)。
4. 在探索函数中，计数 N(s,a) 的作用是什么？
5. 为什么探索函数可以替代 ε-贪婪而无需手动衰减 ε？
6. 如果智能体使用探索函数，在状态 s 如何选择动作？
7. 探索函数中的常数 k 如何影响探索行为？
8. 在真实环境中，探索可能带来负面后果（如机器人掉下悬崖），但仍需要探索，为什么？
9. 什么是“乐观初始化”（optimistic initialization）？它与探索函数有何异同？
10. 如何权衡探索与利用的“后悔”界限？

---

## 第6章 Bayes Nets

### 6.1 Probability Rundown
1. 写出联合分布 P(A,B,C) 的链式法则展开。
2. 给定 P(A,B)=0.1，P(A)=0.2，求 P(B|A)。
3. 写出贝叶斯公式（Bayes' rule）。
4. 什么是条件独立？符号 A ⟂ B | C 的含义是什么？
5. 如果 A ⟂ B | C，那么 P(A|B,C) 等于什么？
6. 什么是“归一化”（normalization）？何时需要？
7. 证明：如果 A ⟂ B 且 B ⟂ C，不能推出 A ⟂ C。（举例）
8. 写出 P(A,B,C) 边缘化得到 P(A,B) 的公式。
9. 如果一个分布不是概率分布（和不等于 1），如何处理？
10. 什么是互斥事件？写出其概率公式。

### 6.2 Probability Inference
1. 什么是推理（inference）？给出一个概率推理的例子。
2. 推理中三类变量是什么？（查询、证据、隐藏）
3. 解释“枚举推理”（inference by enumeration）的步骤。
4. 给定一个包含 4 个变量的联合分布表，分别有 2,2,2,3 个取值，表共有多少行？
5. 如果你有联合分布，是否可以计算任意条件概率？为什么？
6. 在枚举推理中，为什么要“求和消去”隐藏变量？
7. 枚举推理的时间复杂度是多少（用变量数 n 和最大域大小 d 表示）？
8. 如果查询变量有多个，如何计算联合后验 P(Q1,Q2|e)？
9. 枚举推理的缺点是什么？
10. 为什么在大型模型中枚举推理不实际？

### 6.3 Bayesian Network Representation
1. 贝叶斯网络由哪两部分组成？
2. 写出贝叶斯网络表示联合分布的公式（乘积形式）。
3. 贝叶斯网络中的边一定表示因果关系吗？为什么？
4. 在“地震-警报-打电话”网络中，写出 P(¬B, ¬E, +A, +J, ¬M) 的乘积表达式。
5. 为什么贝叶斯网络可以比完整的联合分布表更紧凑？
6. 每个节点的条件概率表（CPT）大小取决于什么？
7. 如果网络有 n 个二值节点，完整联合分布表需要多少参数？如果每个节点最多有 k 个父节点，贝叶斯网络需要多少参数？
8. 贝叶斯网络模型“总是错误的”是什么意思？（教材原话）
9. 什么是 DAG？贝叶斯网络为什么要求无环？
10. 给出一个具有三个节点的贝叶斯网络图，使其表示独立关系 X ⟂ Y。

### 6.4 Structure of Bayes Nets
1. 贝叶斯网络中，一个节点与其非后代节点的条件独立性是什么？
2. 什么是马尔可夫毯（Markov blanket）？包含哪三类节点？
3. 利用条件独立性，证明联合分布因子分解公式 P(X1..Xn)=∏P(Xi|Parents(Xi))。
4. 为什么拓扑排序（parents before children）是重要的？
5. 在 alarm 网络中，J（JohnCalls）的马尔可夫毯包括哪些节点？
6. 给定贝叶斯网络，是否能直接从结构看出两个节点是否绝对独立？
7. 如果节点有多个父节点，CPT 的规模随父节点数如何增长？
8. 为什么马尔可夫毯对于 Gibbs 采样很重要？
9. 在贝叶斯网络中，如何判断一个节点是否条件独立于另一个节点（给定某些证据）？需要什么工具？
10. 如果网络是完全连通的（所有节点之间都有边），条件独立性还成立吗？

### 6.5 D-Separation
1. D-分离（d-separation）的定义是什么？
2. 列举三种基本的三节点图结构：因果链、共同原因、共同效果。各自的条件独立特性分别是什么？
3. 在因果链 X → Y → Z 中，X 和 Z 是否独立？如果不独立，什么条件下独立？
4. 在共同原因 X ← Y → Z 中，X 和 Z 是否独立？条件独立于什么？
5. 在共同效果 X → Y ← Z 中，X 和 Z 是否独立？什么条件下它们可能相关？
6. 为什么共同效果被称为“v-结构”？其特殊性质是什么？
7. 如果 Y 的一个后代节点被观测，共同效果结构中的 X 和 Z 会怎样？
8. 给出一个贝叶斯网络，判断给定某观察集时 A 和 B 是否 d-分离的具体算法步骤。
9. 如果一条路径上有一个“活跃”的三元组，但其他三元组不活跃，整条路径是否 d-连接？
10. 解释为何在 d-分离算法中要“激活”共同效果节点的后代。

### 6.6 Exact Inference in Bayes Nets
1. 变量消除（variable elimination）与枚举推理的核心区别是什么？
2. 变量消除的“因子”（factor）是什么？如何得到？
3. 写出变量消除的算法伪代码。
4. 为什么变量消除中因子的最大规模决定了效率？
5. 给定网络 T → C, T → S, C,S → E，要计算 P(T|+e)，写出最有效的变量消除顺序。
6. 变量消除的时间复杂度取决于什么？
7. 什么是“因子相乘”？它与概率的乘积有何不同？
8. 在变量消除中，为什么要先“加入”然后“求和消除”一个变量？
9. 变量消除是否总能找到精确解？与枚举推理的回答相同吗？
10. 如果网络很大但树宽（treewidth）很小，变量消除可行吗？

### 6.7 Approximate Inference in Bayes Nets: Sampling
1. 什么是先验采样（prior sampling）？它如何生成样本？
2. 给定 P(T)=0.9, P(C|T)=0.8, P(C|¬T)=0.1，如果随机数 r=0.2 决定 T，r=0.7 决定 C，写出一次采样结果。
3. 拒绝采样（rejection sampling）如何利用证据？
4. 拒绝采样在证据概率很低时有什么问题？
5. 似然加权（likelihood weighting）如何避免了拒绝采样丢弃样本的问题？
6. 似然加权中样本的权重是如何计算的？
7. 计算 P(T|+c) 时，如果采样得到 t=true，并且 P(+c|t)=0.3，该样本权重是多少？
8. Gibbs 采样的基本思想是什么？它如何更新变量？
9. Gibbs 采样中为什么要用马尔可夫毯？
10. 与先验采样相比，为什么似然加权通常更高效？

### 6.8 Summary
编写该小节题目时，发现教材6.8仅有总结性文字。请结合本章全部内容自行出题如下：
1. 贝叶斯网络的主要优点是什么？
2. 精确推理与近似推理分别适用于什么场景？
3. 写出枚举推理、变量消除、似然加权、Gibbs 采样的输出（分别返回什么）。
4. 什么情况下枚举推理不可避免指数爆炸？
5. 采样方法的误差如何随着样本数增加而减小？
6. 为什么 Gibbs 采样有时可能陷入局部模式？
7. 变量消除中的“消元顺序”对计算量有何影响？
8. 列举至少两个启发式来选择好的消元顺序。
9. 对于树状结构的贝叶斯网络，变量消除的复杂度是多少？
10. 贝叶斯网络能否表示任意概率分布？为什么？

---

## 第7章 Decision Networks and VPIs

### 7.1 Utilities
1. 理性智能体应遵循什么原则？（最高期望效用原则）
2. 什么是偏好（preference）？符号 A ≻ B 的含义？
3. 理性偏好的五个公理是什么？
4. 什么是彩票（lottery）？如何表示？
5. 如果 U( $1000)=100, U($0)=0，那么彩票 L=[0.5,$1000;0.5,$0] 的期望效用是多少？
6. 风险中性（risk-neutral）、风险厌恶（risk-averse）、风险寻求（risk-seeking）的定义分别是什么？举例说明。
7. 证明：如果效用函数是线性的，则智能体是风险中性的。
8. 连续性公理（continuity）的直观意义是什么？
9. 期望效用定理（Expected Utility Theorem）表明什么？
10. 为什么一个理性智能体必须满足可传递性（transitivity）？

### 7.2 Decision Networks
1. 决策网络包含哪三类节点？分别用什么形状表示？
2. 机会节点（chance node）与贝叶斯网络中的节点有何关系？
3. 行动节点（action node）有什么特点？
4. 效用节点（utility node）的父节点可以是哪些类型？
5. 给定决策网络，如何计算在证据 e 下采取行动 a 的期望效用？
6. 什么是 MEU（maximum expected utility）？如何计算？
7. 写出在观察到证据 e 后，选择最优行动的决策规则。
8. 教材中“是否带伞”的决策网络，其机会节点是什么？行动节点是什么？
9. 什么是结果树（outcome tree）？与决策网络的关系？
10. 决策网络与影响图（influence diagram）通常被视为同义词吗？

### 7.3 The Value of Perfect Information
1. VPI 的定义是什么？
2. 写出 VPI(E' | e) 的公式。
3. 为什么 VPI 总是非负的？
4. 如果新证据 E' 与决策无关，VPI 是多少？
5. 为什么 VPI 不具有可加性？举例说明。
6. VPI 的“顺序无关性”是什么意思？
7. 假设当前 MEU(e)=70，如果观测到 F=good 时 MEU=95，F=bad 时 MEU=53，P(good)=0.59，P(bad)=0.41，求 VPI(F|e)。
8. 如果观测新证据需要成本 c，智能体何时应该观测？
9. 如果观测证据后可能得到更差的 MEU（比如由于概率分布），VPI 可能为负吗？为什么？
10. 完美信息（perfect information）与部分信息（partial information）的区别是什么？

---

## 第8章 HMMs

### 8.1 Markov Models
1. 马尔可夫模型的基本结构是什么？（图）
2. 写出马尔可夫链中 P(W0, W1, ..., Wn) 的分解公式。
3. 什么是马尔可夫性质（Markov property）？
4. 为什么马尔可夫模型通常假设转移模型是平稳的（stationary）？
5. 给定初始分布 P(W0) 和转移矩阵，如何用“迷你前向算法”计算 P(W1)？
6. 如果 P(W0=sun)=0.8, P(W0=rain)=0.2, 转移矩阵为 [[0.6,0.4],[0.1,0.9]]，求 P(W1=rain)。
7. 什么是平稳分布（stationary distribution）？如何求解？
8. 从上一题的转移矩阵，求解平稳分布。
9. 如果转移矩阵是单位矩阵，平稳分布是什么？
10. 迷你前向算法的时间复杂度是多少（用状态数 n 和时间步 t 表示）？

### 8.2 Hidden Markov Models
1. HMM 相比普通马尔可夫模型多了哪些节点？
2. 写出 HMM 的联合分布 P(W0..Wt, F1..Ft) 分解式。
3. 什么是传感器模型（sensor model）？通常假设什么性质？
4. 信念状态 B(Wi) 的定义是什么？
5. 前向算法（forward algorithm）的时间更新和观测更新的公式分别是什么？
6. 给定初始 B(W0)=[0.8,0.2]，转移矩阵 [[0.6,0.4],[0.1,0.9]]，传感器模型 P(good|sun)=0.8,P(good|rain)=0.3，观测到 F1=good，计算 B(W1) 的未归一化值，再归一化。
7. 为什么在前向算法中要推迟归一化？
8. 前向算法的时间复杂度如何随状态数增长？
9. HMM 中状态变量个数随时间增长，为什么还能在线计算？
10. 如果 HMM 没有观测（传感器模型恒为 1），它退化为什么？

### 8.3 Viterbi Algorithm
1. Viterbi 算法解决什么问题？（最可能状态序列）
2. 写出 Viterbi 递归关系中，从 t-1 到 t 的最大概率路径的计算公式。
3. 为什么 Viterbi 算法也采用动态规划？
4. Viterbi 算法中需要存储什么信息以便回溯？
5. 给定一个两状态 HMM，t=1 到 t=3 的观测序列，如何找到最可能的隐藏状态序列？
6. Viterbi 算法与 forward 算法的时间复杂度比较如何？
7. 为什么 Viterbi 算法被称为“解码”过程？
8. 如果用 Viterbi 找到的最可能路径概率非常低，可能说明什么？
9. Viterbi 算法中，路径概率的乘积可能导致数值下溢，如何避免？
10. Viterbi 算法是否保证找到全局最优序列？为什么？

### 8.4 Particle Filtering
1. 粒子滤波（particle filtering）的基本思想是什么？
2. 粒子滤波中，一个粒子代表什么？
3. 粒子滤波的“时间更新”步骤如何实现？
4. 粒子滤波的“观测更新”步骤如何给粒子加权？
5. 粒子滤波中如何解决“粒子退化”问题？（重采样）
6. 为什么粒子滤波能够近似 HMM 的信念状态？
7. 如果只有 10 个粒子而状态有 1000 个，会产生什么影响？
8. 粒子滤波的时间复杂度与粒子数 M 和状态维度 d 的关系如何？
9. 与精确前向算法相比，粒子滤波的主要优势是什么？
10. 为什么粒子滤波的观测更新与似然加权非常相似？

### 8.5 Summary
自行出题：
1. 马尔可夫模型和隐马尔可夫模型各自解决什么问题？
2. 写出前向算法的核心递归关系。
3. 写出 Viterbi 算法的核心递归关系。
4. 粒子滤波的三个步骤是什么？
5. 当状态空间很大时，精确 HMM 推理不可行，可以采用什么方法？
6. 什么情况下马尔可夫链的平稳分布可能不存在？
7. 如何评价粒子滤波估计的准确性？
8. 教材中的天气 HMM 示例，如果连续两天观测到 good，信念状态会如何变化？
9. 比较 Viterbi 路径和 forward 算法的边缘概率序列有何不同用途。
10. 设计一个简单的 HMM 示例（2 个状态，2 个观测），计算一次 forward 步骤。

---

## 第9章 ML

### 9.1 Machine Learning
1. 什么是监督学习（supervised learning）？举例。
2. 什么是无监督学习（unsupervised learning）？举例。
3. 训练集（training set）、验证集（validation set）、测试集（test set）各自的作用是什么？
4. 什么是超参数（hyperparameter）？如何选择？
5. 为什么测试集必须在模型训练完成后才能使用？
6. 过拟合（overfitting）是什么意思？
7. 什么是特征工程（feature engineering）？
8. 在机器学习中，“训练”和“推理”分别指什么？
9. 为什么需要将数据划分为三个子集，而不是只用训练集和测试集？
10. 如果验证集准确率高但测试集准确率低，可能是什么问题？

### 9.2 Naive Bayes
1. Naive Bayes 做了哪个关键假设？
2. 写出 Naive Bayes 分类器的决策规则（argmax 形式）。
3. 为什么朴素贝叶斯被称为“朴素”？
4. 在朴素贝叶斯中，特征之间是否独立？条件于什么独立？
5. 给定 P(Y=spam)=0.3, P(Y=ham)=0.7，两个特征的条件概率表，如何计算一个邮件是垃圾邮件的后验？
6. 什么是 MLE（最大似然估计）？写出 Bernoulli 分布下参数 θ 的 MLE 表达式。
7. 拉普拉斯平滑（Laplace smoothing）如何解决零概率问题？
8. 如果使用拉普拉斯平滑，k=1，对于二值特征，P(F=1|Y=spam) 的估计公式是什么？
9. 朴素贝叶斯需要估计多少参数（假设 n 个特征，每个二值，Y 二值）？
10. 朴素贝叶斯能处理连续特征吗？如何扩展？

### 9.3 Perceptron
1. 线性分类器的激活函数（activation）如何计算？写出数学表达式。
2. 决策边界（decision boundary）是什么？对于一个线性分类器，它是什么形状？
3. 感知机（perceptron）的基本更新规则是什么？（错误时）
4. 如果真实标签 y*=+1，预测 y=-1，权重如何更新？
5. 感知机的收敛条件是什么？
6. 为什么感知机需要偏置（bias）项？如何加入？
7. 感知机能否解决 XOR 问题？为什么？
8. 写出多类感知机的更新规则（当预测错误时）。
9. 感知机算法是否保证收敛到线性可分的训练数据？
10. 在感知机中，权重更新的几何意义是什么？

### 9.4 Binary Perceptron
注意：教材 9.4 为 Binary Perceptron（实际上 9.3 和 9.4 内容衔接，这里合并出题，但仍按小节顺序）
为满足每个小节 10 题，该小节题目参考 9.3 已经涵盖。补充如下：
1. 给定特征向量 x=[1,2]，权重 w=[0.5, -1]，无偏置，激活值是多少？分类结果？
2. 如果真实标签为 +1，而上述分类错误，更新后的新权重是什么？
3. 感知机每次迭代遍历训练集一次，称为一个 epoch。通常需要多少个 epoch？
4. 如果数据不是线性可分的，感知机算法会怎样？
5. 感知机与逻辑回归的主要区别是什么？
6. 为什么感知机使用 sign 函数，而逻辑回归使用 sigmoid？
7. 在在线学习中，感知机可以逐点更新，这有什么优点？
8. 如何处理多类分类？一对多（one-vs-rest）还是多类感知机？
9. 感知机与支持向量机有何关系？
10. 感知机的损失函数是什么？（称为“合页损失”的变种）

### 9.5 Linear Regression
1. 线性回归的目标是预测什么类型的变量？
2. 写出线性回归的假设函数 h_w(x)。
3. 线性回归常用的损失函数是什么？写出表达式。
4. 最小二乘解的闭式（closed-form）是什么？用矩阵形式写出。
5. 如果 X^T X 不可逆，通常是什么原因？如何解决？
6. 梯度下降用于线性回归的更新公式是什么？
7. 为什么线性回归有时需要添加正则项（如岭回归）？
8. 什么是过拟合？如何通过验证集检测？
9. 线性回归中，特征缩放（feature scaling）为什么有用？
10. 给定两个点 (1,2) 和 (2,3)，求线性回归参数 w0, w1。

### 9.6 Optimization
1. 梯度上升与梯度下降的区别是什么？
2. 写出梯度下降的参数更新公式。
3. 学习率（learning rate）α 太大或太小会有什么问题？
4. 什么是随机梯度下降（SGD）？它如何估算梯度？
5. 批量梯度下降（batch GD）与 SGD 的优缺点各是什么？
6. 小批量梯度下降（mini-batch GD）的批量大小如何选择？
7. 为什么凸函数的梯度下降能保证收敛到全局最优？
8. 什么是学习率衰减（learning rate decay）？
9. 梯度下降的收敛判据通常有哪些？
10. 对于线性回归，梯度下降与闭式解在什么情况下更优？

### 9.7 Logistic Regression
1. 逻辑回归解决什么问题？输出是什么范围？
2. 写出逻辑函数（sigmoid）的公式。
3. 逻辑回归如何将线性组合转换为概率？
4. 写出逻辑回归的似然函数（单个样本）。
5. 逻辑回归的损失函数通常采用什么？与线性回归有何不同？
6. 逻辑回归中，为什么不能用最小二乘损失直接优化？
7. 写出逻辑回归梯度下降的梯度表达式（对权重 wi）。
8. 逻辑回归的决策边界是什么形状？
9. 多类逻辑回归使用什么函数代替 sigmoid？
10. 逻辑回归的输出来源于指数族分布中的伯努利分布，对吗？

### 9.8 Multi-Class Logistic Regression
1. 多类逻辑回归使用 softmax 函数，写出 softmax 公式。
2. 假设有三个类别，特征向量 f(x)，权重向量 w1,w2,w3，计算 P(y=2|f)。
3. 多类逻辑回归中，参数数量是多少（特征数 d，类别数 K）？
4. 写出多类逻辑回归的对数似然函数。
5. 为什么 softmax 的指数形式可以保证概率非负且和为 1？
6. 多类逻辑回归的梯度表达式是什么？（对 w_j）
7. 如何用梯度上升更新 w_j？
8. 多类逻辑回归与一对多（one-vs-rest）逻辑回归有何异同？
9. 在 softmax 中，如果对所有 w_j 加上同一个向量，概率会变吗？为什么？
10. 为了避免参数冗余，通常对 w 进行什么正则化？

### 9.9 Neural Networks: Motivation
1. 为什么单层感知机不能学习非线性决策边界？
2. 如何通过添加特征（如 x^2）使原本非线性可分的问题变为线性可分？
3. 多层感知机（MLP）与单层感知机相比，表达能力有何提升？
4. 什么是通用逼近定理（Universal Approximation Theorem）？
5. 为什么在多层网络中需要非线性激活函数？
6. 常用的非线性激活函数有哪些？写出至少三个。
7. ReLU 函数是什么？写出其公式。
8. Sigmoid 函数有什么缺点？梯度消失是什么？
9. 神经网络中的“层”指的是什么？
10. 神经网络如何通过反向传播（backpropagation）更新权重？（简述思想）

---

## 第10章 Logic

### 10.1 A Knowledge Based Agent
1. 知识型智能体（knowledge-based agent）维护什么数据结构？
2. 知识库（KB）中包含什么？
3. 知识型智能体与强化学习智能体的核心不同点是什么？
4. 什么是逻辑推理（logical inference）？
5. 举例说明一个知识型智能体如何利用事实进行决策。
6. 知识库中的句子可以是事实还是规则？两者区别。
7. 为什么知识型智能体需要“告诉”（tell）和“询问”（ask）操作？
8. 陈述“如果地面湿了，则可能下雨”在知识库中如何表示？
9. 知识型智能体的“模型”（model）指什么？
10. 知识工程（knowledge engineering）的主要挑战是什么？

### 10.2 The Language of Logic
1. 逻辑语言的语法（syntax）与语义（semantics）各指什么？
2. 写出五个逻辑连接词及其符号。
3. 写出 P ∧ Q 的真值表。
4. 写出 P ⇒ Q 的真值表。什么情况下蕴含式为假？
5. 什么是文字（literal）？举例。
6. 什么是合取（conjunction）？什么是析取（disjunction）？
7. 双条件 P ⇔ Q 等价于哪两个蕴含式的合取？
8. 如果 P 为真，Q 为假，那么 P ∨ Q 和 P ⇒ Q 的真值分别是什么？
9. 逻辑等价关系：德摩根律是什么？
10. 将句子“如果下雨则地湿”用逻辑符号表示。

### 10.3 Propositional Logic
1. 命题逻辑中什么是“命题符号”？举例。
2. 一个模型（model）在命题逻辑中是什么？
3. 对于 n 个命题符号，有多少个可能模型？
4. 什么叫“有效”（valid）、“可满足”（satisfiable）、“不可满足”（unsatisfiable）？举例。
5. 写出 CNF（合取范式）的一般形式。
6. 将 ¬(A ∨ B) 转换为 CNF（使用德摩根律）。
7. 将 A ⇔ B 转换为 CNF（按教材步骤）。
8. 证明：A ∧ (A ⇒ B) ⇒ B 是有效的（使用真值表或逻辑推导）。
9. 什么是“重言式”（tautology）？
10. 如果 KB 蕴含 q，那么 KB ∧ ¬q 是什么逻辑状态？

### 10.4 Propositional Logical Inference
1. 蕴含（entailment）的定义是什么？用模型集合表示。
2. 证明 A ⊨ B 的两种方法是什么？
3. 什么是模型检测（model checking）？时间复杂度是多少？
4. 为什么命题逻辑的蕴含问题（co-NP-complete）很难？
5. DPLL 算法主要用于解决什么问题？
6. DPLL 算法的三个关键启发式是什么？
7. 早期终止（early termination）如何工作？
8. 纯符号启发式（pure symbol heuristic）的例子。
9. 单元子句启发式（unit clause heuristic）的例子。
10. 如果 DPLL 返回 false，说明原 CNF 句子是什么状态？

### 10.5 Theorem Proving
1. 什么是归结（resolution）规则？写出形式。
2. 归结法如何用于证明 KB ⊨ q？
3. 前向链接（forward chaining）适用于什么形式的 KB？（霍恩子句）
4. 前向链接的 agenda 结构是什么？
5. 给定 KB: A, A ⇒ B, B ⇒ C，用前向链接证明 C 的步骤。
6. 后向链接（backward chaining）与前进方向有何不同？
7. 什么是霍恩子句（Horn clause）？举例。
8. 为什么前向链接在霍恩子句下是完备的？
9. 归结法的完备性如何？
10. 如果知识库中有 ¬P ∨ Q 和 ¬Q ∨ R，可以归结出什么？

### 10.6 Forward Chaining
（教材给出 Forward Chaining 算法及示例）
1. 前向链接算法中“count”数组的作用是什么？
2. “agenda”中存放什么？
3. 给定 KB: A, A→B, B∧C→D, C，初始 agenda 是什么？
4. 在示例 10.6.1 中，从 A 开始，经过几次迭代得到 Q？
5. 如果知识库中有循环（如 A→B, B→A），前向链接会终止吗？
6. 前向链接的时间复杂度与 KB 大小有何关系？
7. 前向链接能否处理含有否定（¬）的规则？为什么？
8. 前向链接是否保证找到所有可推出的结论？
9. 如果 query 是 C，但 C 不可推出，前向链接会如何结束？
10. 为什么前向链接算法需要“inferred”表防止重复推导？

### 10.7 First Order Logic
1. 一阶逻辑（FOL）比命题逻辑增加了什么？
2. 什么是项（term）？举例。
3. 什么是谓词（predicate）？举例。
4. 全称量词（∀）和存在量词（∃）的含义是什么？
5. 将“所有人都会死”用一阶逻辑表示。
6. 将“有人喜欢巧克力”用一阶逻辑表示。
7. 写出 FOL 中量词的德摩根定律。
8. 函数符号（function symbol）与谓词符号的区别是什么？
9. 什么是原子句子（atomic sentence）？
10. 如何表示“x 的父亲是 y”？用 Father(x,y) 还是 Father(x)=y？

### 10.8 First Order Logical Inference
1. 什么是命题化（propositionalization）？优点和缺点？
2. Herbrand 定理对命题化有何保证？
3. 什么是广义假言推理（Generalized Modus Ponens）？
4. 什么是合一（unification）？举例。
5. 给定规则 ∀x (Person(x) ∧ Rich(x) ⇒ Happy(x)) 和事实 Person(John), Rich(John)，合一后得到什么？
6. 前向链接在一阶逻辑中如何应用？
7. 存在量词如何通过 Skolemization 消除？
8. 一阶逻辑归结（resolution）需要什么预处理步骤？
9. 为什么一阶逻辑的推理是半可判定的？
10. 什么是“提升”（lifting）？

### 10.9 Logical Agents
1. 什么是反事实（fluent）？举例。
2. 写出后继状态公理（successor-state axiom）的一般形式。
3. 如果智能体只知道初始状态和转移规则，如何规划达到目标？
4. 在逻辑规划中，目标可以表示为某时间步的状态条件（如 At(Oasis, T)）。如何求解？
5. 动作的“效果公理”（effect axiom）和“框架公理”（frame axiom）分别是什么？
6. 为什么需要框架公理？如何用后继状态公理解决框架问题？
7. 为什么要将时间显式地作为参数？
8. 在逻辑规划中，如果动作有并发可能，如何处理？
9. 状态估计算法（如滤波）在一阶逻辑中如何实现？
10. 比较逻辑智能体与强化学习智能体的优缺点。

### 10.10 Summary
自行出题（基于整章）：
1. 命题逻辑与一阶逻辑的主要区别是什么？
2. DPLL 算法用于判断什么问题的可满足性？
3. 前向链接适用于什么类型的知识库？
4. 广义假言推理与命题假言推理的区别是什么？
5. 写出归结规则（propositional resolution）的标准形式。
6. 在逻辑智能体中，如何表示时间？
7. 什么是“知识工程”？主要步骤有哪些？
8. 为什么一阶逻辑的表达能力更强，但推理更困难？
9. 举例说明一个现实问题更适合用一阶逻辑而非命题逻辑建模。
10. 本节（第10章）学到的主要结论是什么？

---

# 答案与解释

以下为每道题的简要答案或关键解释。由于题目数量多，答案力求简洁但覆盖核心。

## 第1章 Search

### 1.1 Agents
1. 理性智能体：总是选择能最大化期望效用的动作，基于其知识和目标。
2. 反射型只基于当前感知；规划型使用世界模型模拟未来后果。
3. Performance measure, Environment, Actuators, Sensors。例如自动驾驶：性能指标为安全到达，环境为道路，执行器为方向盘等，传感器为摄像头。
4. 部分可观测：扑克牌（不知对手牌）；完全可观测：围棋（全局可见）。
5. 随机：同一动作可能产生不同结果；确定性：结果唯一。
6. 避免被对手预测而利用。
7. 静态：环境不随智能体动作而改变（除智能体自身）；动态：环境自动变化。
8. 已知物理意味着知道转移模型，可直接规划。
9. 不需要，因为完全可观测且确定性，初始状态已知，动作效果已知，无需感知反馈（但仍需确认是否达到目标）。
10. 预测动作后果，选择最佳行动。

### 1.2 State Spaces and Search Problems
1. 状态空间、动作集、转移模型、动作代价、初始状态、目标测试。
2. 世界状态包含所有信息，搜索状态只包含规划所需信息。
3. 吃豆子问题中不必记录所有豆子的确切位置，只需记录哪些豆子还在（布尔向量）。
4. 120 × 4 × (12×12) × 2^30
5. 状态空间图每个状态只出现一次；搜索树中一条路径对应一个节点，状态可重复。
6. 因为状态数巨大（如10^13以上）。
7. 因为不同路径可以到达同一状态。
8. 搜索树大小 ≥ 状态空间图大小。
9. 只存储当前 frontier 节点，动态调用 Result(s,a) 生成子节点。
10. 从初始状态到目标状态的行动序列，在搜索树中表现为从根到目标节点的路径。

### 1.3 Uninformed Search
1. 初始化 frontier，循环弹出节点，若是目标则返回，否则扩展子节点加入 frontier。
2. LIFO 栈，因为要深度优先。
3. DFS 不完备（有环时可能无限深入）；不最优（不关心代价）。
4. 时间 O(b^m)，空间 O(bm)。
5. FIFO 队列，因为要宽度优先。
6. 当所有边代价相等时最优（退化到 UCS）。
7. 时间 O(b^s)，空间 O(b^s)（s为最浅解深度）。
8. 优先队列（堆），优先级为路径代价 g(n)。
9. 完备且最优（非负边权）；负边权会破坏最优性。
10. O(b^{C*/ε})。

### 1.4 Informed Search
1. 启发式 h(n) 估计从 n 到目标的最短路径代价，常来自松弛问题。
2. |x1-x2|+|y1-y2|；无墙的曼哈顿距离。
3. 选择 h(n) 最小的节点；不完备也不最优。
4. f(n)=g(n)+h(n)，估计总代价。
5. 0 ≤ h(n) ≤ h*(n)（h*为真实最小代价）。
6. 取任意最优目标 A 和次优 B，证明 A 的所有祖先 n 满足 f(n)<f(B)，故先于 B 扩展。
7. 此时 f(n)=1，退化为 BFS（常数代价）。
8. 维护 closed set，已扩展状态不再扩展。
9. 如图中例子，通过不同路径到达相同状态可能第二次路径更优，但被 closed set 阻止。
10. 对多个可采纳启发式取 max，得到更紧的下界，优势（dominate）。

### 1.5 Local Search
1. 只关心目标状态本身，不关心路径。
2. 地形图高度为目标函数值；全局最大、局部最大、平坦区域、肩膀（缓慢上升）。
3. 易陷局部最大/平坦，可用随机重启、随机爬山、侧向移动。
4. 随机爬山从上升移动中随机选一个。
5. 高温时允许差移动（探索），降温后逐步趋于爬山（利用）。
6. e^{ΔE/T}，接受概率随温度降低而减小。
7. 局部束搜索每代从所有线程的后继中选出 k 个最好的，线程间可交流。
8. 个体为棋盘布局，适应度为非攻击皇后对数。
9. 交叉混合父母基因，变异随机改变位点以保持多样性。
10. 最小冲突启发式几乎常数时间解决百万皇后问题。

## 第2章 CSPs

### 2.1 Constraint Satisfaction Problems
1. 变量、值域、约束。
2. 变量：每个格子 Xij；值域 {0,1}；约束：每行最多一个 1，每列最多一个 1，对角线等。
3. CSP 是识别问题，检查是否存在满足约束的赋值；搜索问题要找路径。
4. 无多项式时间算法，完全赋值数量 d^N。
5. 部分赋值是只给部分变量赋值；后继函数给下一个未赋值变量赋一个值。
6. 节点为变量，边为二元约束。
7. 三元约束用超边或因子图；例如三个变量的和约束。
8. 相邻区域之间连边。
9. 树结构可线性求解，割集可分解。
10. WA≠NT, WA≠SA, NT≠SA, Q≠SA, NSW≠SA, V≠SA, Q≠NSW等。

### 2.2 Solving Constraint Satisfaction Problems
1. 回溯搜索 = DFS + 检查约束，只赋值不冲突的值。
2. 避免大量无效子树。
3. 赋值顺序不影响解集（交换性）。
4. 回溯到上一个变量，尝试其他值。
5. DFS 会尝试全红导致冲突；回溯搜索每次检查约束。
6. O(d^N) 最坏情况。
7. MRV: 选剩余值最少的变量；LCV: 选约束最少的值。
8. 回溯到前一个变量。
9. 完备，只要搜索完整空间。
10. 需要运行额外推理（如前向检查）来确定哪个值约束最少。

### 2.3 Filtering
1. 前向检查：当变量赋值后，删除邻接未赋值变量中违反约束的值。
2. 弧一致性更强，传播更广。
3. 维护队列，对每条弧 (X->Y)，删除 X 中无 Y 支持的值，若 X 域改变则加入所有指向 X 的弧。
4. O(ed^3)。
5. SA 会删除红色和绿色，只剩蓝色。
6. 前向检查更快但修剪少；弧一致性更彻底但计算贵。
7. k-一致性：任意 k-1 个变量的一致赋值可扩展至第 k 个变量；2-一致性=弧一致性。
8. 所有 j≤k 都一致。
9. 无解，需回溯。
10. 对 X_i 的每个值 x，检查是否存在 X_j 的值 y 使约束满足；若无则删除 x，返回 true。

### 2.4 Ordering
1. 最约束变量最可能快速失败，尽早赋值减少分支。
2. Y（域大小1）。
3. 选择对剩余变量约束最小的值，需模拟赋值后域大小。
4. O(nd^2)（线性）。
5. 先向后弧一致性（从叶子到根），再向前赋值（从根到叶子）。
6. 割集是移除后使图成树的节点集；对割集所有赋值，对剩余树求解。
7. O(d^c (n-c)d^2)。
8. 在紧密约束中有效，松散约束中无效。
9. 可以，先 MRV 选变量，再用 LCV 选值。
10. 不需要回溯，因弧一致性保证父节点任何值都有孩子值一致。

### 2.5 Local Search
1. 随机完整赋值。
2. 每次选一个冲突变量，改为使冲突数最小的值。
3. 如 4 皇后，某列移动皇后到冲突最少行。
4. 大部分随机初始在解附近可迅速收敛。
5. 不完备，可能局部最优。
6. 约束密度在某个比值时从易解变难解。
7. 爬山选提升目标最多的移动；min-conflicts 是特殊形式。
8. 概率接受降低目标值的移动。
9. 适应度 = 满足的约束数（或冲突的负数）。
10. 对于随机生成的 CSP，解密集分布在空间中。

## 第3章 Games

### 3.1 Games
1. 零和：一方收益等于另一方损失（如象棋）。
2. 已解：任何局面下最优结果已知（如井字棋、跳棋）。
3. 策略（policy），因为对手行动不确定，需根据状态决策。
4. 初始状态、玩家、动作、转移模型、终止测试、效用函数。
5. 轮到谁走步，该节点就由谁控制。
6. 因为对手行为不确定，不能预先确定结果。
7. 策略是状态到动作的函数；计划是固定动作序列。
8. Go 分支因子大且很难设计评估函数。
9. 随机博弈（如掷骰子）。
10. 洗牌算法（shuffle）在多智能体中用于随机化以避免被预测。

### 3.2 Minimax
1. 对手总是采取使己方效用最小的动作（最优）。
2. V(s) = max_{a} V(Result(s,a)) 对 max 节点；min 对 min 节点。
3. 期望线：max 节点向上三角形；min 节点向下三角形。
4. 式见教材。
5. 左边 min=3，中间 min=2，右边 min=2，根 max=3。
6. 剪掉不可能影响最终决策的分支。
7. O(b^{m/2})（最佳顺序）。
8. 若先探索最优子节点，剪枝增多。
9. 估计非终端节点的值，用于深度限制搜索。
10. 特征：己方棋子数、对方棋子数、中心控制、象优势等，权重正比重要性。

### 3.3 Expectimax
1. 有 chance 节点（期望值）而非 min 节点。
2. V(s) = Σ P(s'|s) V(s')。
3. (5+10+15)/3 = 10。
4. 当对手概率分布退化为 1 给最小子时。
5. 环境随机性（如掷骰子）或对手行为随机性。
6. 不能简单剪枝，因为一个极端值会影响期望。
7. EU = 0.6*V(left) + 0.4*V(right)。
8. Minimax 假设对手最优，但随机对手可能让高风险动作实际更好。
9. 可构建 max→chance→min 多层混合。
10. 多个幽灵依次移动，可建模为 max（Pacman）后跟多个 min（幽灵）或 chance（若随机）。

### 3.4 General Games
1. 效用元组，每个分量对应一个智能体。
2. 最大化自己的分量。
3. 红层 max 红：从 (5,2,5) 和 (3,1,6) 选 (5,2,5)（红分量5>3），绿层看 (5,2,5) 和 (4,4,4) 中绿分量 max -> (4,4,4)（4>2），蓝层 max 蓝：(4,4,4) 胜出，根值为 (4,4,4)。
4. 因为最大化自己效用可能也提高别人效用。
5. 合作博弈，可视为单智能体。
6. 帕累托最优：无法在不损害他人下改善自己；纳什均衡：无人可单方面改变策略获更好收益。
7. 各智能体目标不同，不能简单交替 min-max。
8. 承诺可改变均衡。
9. 两家公司竞争市场份额，可分蛋糕。
10. 混合策略是概率分布，零和中用于使对手无法预测。

### 3.5 Monte Carlo Tree Search
1. 评估通过 rollout，选择性搜索。
2. 从当前状态按策略（如随机）模拟至终局，统计赢率。
3. 用 UCB 公式平衡探索与利用。
4. UCB1 = U/N + C √(log N_parent / N)，U 为获胜次数，N 为访问次数。
5. 平衡因子，C 大更探索。
6. 选择（从根用 UCB 选叶子），扩展（加新节点），模拟（rollout），回传（更新路径上统计）。
7. 不用全展开，聚焦有希望分支，且可并行。
8. 不需要启发式，但可用领域知识改进 rollout。
9. 趋近于 Minimax。
10. 优点：无需评估函数，适于大分支；缺点：理论保证不如 α-β，需要大量模拟。

## 第4章 MDPs

### 4.1 Markov Decision Processes
1. 状态集 S，动作集 A，转移函数 T，奖励函数 R，折扣因子 γ，起始状态，可能终止状态。
2. T(s,a,s') = P(s'|s,a)。
3. 立即奖励，总回报是折扣奖励和。
4. (s,a) 表示从 s 采取 a 后的过渡状态（Q-state）。
5. γ=0 只关心当前奖励，γ=1 无限时长无折扣。
6. 避免无穷和发散，且经济上远期奖励价值低。
7. 1+0.9+0.81+... = 10（等比求和）。
8. 吸收状态：无出边，通常奖励 0。
9. 不一定，转移函数概率性。
10. 略。

### 4.2 Solving Markov Decision Processes
1. 求最优策略 π*: S→A 最大化期望累积奖励。
2. 状态到动作的映射。
3. 策略定义了反射型行为（只根据当前状态选动作）。
4. V*(s)=max_a Σ_s' T(s,a,s')[R(s,a,s')+γ V*(s')]。
5. Q*(s,a)= Σ_s' T(s,a,s')[R(s,a,s')+γ V*(s')]；V*(s)=max_a Q*(s,a)。
6. 将原问题分解为子问题递归。
7. V*(s)=max_a [R(s,a)+γ V*(Result(s,a))]。
8. π*(s)=argmax_a Q*(s,a)。
9. 是，贝尔曼方程是最优性的充要条件。
10. 因为 MDP 随机，需要根据状态实时决策。

### 4.3 Value Iteration
1. V0(s)=0（0步无奖励）。
2. V_{k+1}(s)=max_a Σ_s' T(s,a,s')[R+γ V_k(s')]。
3. max_s |V_{k+1}(s)-V_k(s)| < 某小阈值。
4. B 定义为右端算子；它满足 ||BV - BV'||_∞ ≤ γ ||V-V'||_∞，故为收缩。
5. 收敛到唯一不动点 V*。
6. 需要 V2(s') 来计算 V3(s)。
7. π*(s)=argmax_a Σ_s' T(s,a,s')[R+γ V*(s')]。
8. π*(s)=argmax_a Q*(s,a)，一步 argmax 即可。
9. O(|S|^2 |A|) 若遍历所有 s'。
10. 略。

### 4.4 Policy Iteration
1. 策略迭代先评估当前策略再改进；值迭代直接更新值。
2. 可以是随机初始，越接近收敛越快。
3. 解出 V^π 满足 V^π(s)= Σ_s' T(s,π(s),s')[R+γ V^π(s')]。
4. 线性方程组求解比多次迭代快。
5. π_{i+1}(s)=argmax_a Σ_s' T(s,a,s')[R+γ V^{π_i}(s')]。
6. 当 π_{i+1}=π_i。
7. 迭代策略评估（多次值迭代）。
8. 策略空间小于值空间（通常策略早收敛）。
9. V(cool)=1+0.5V(cool)；V(warm)=0.5(1+0.5V(cool))+0.5(1+0.5V(warm))。
10. 每次迭代策略评估 O(|S|^3) 解方程，但迭代次数少。

## 第5章 RL

### 5.1 Reinforcement Learning
1. 离线规划已知模型；在线规划通过交互学习。
2. 探索：尝试未知动作获取信息；利用：使用已知最优动作最大化奖励。
3. 样本 (s,a,s',r)；情节是从起始到终止的状态动作序列。
4. 模型基显式学习 T,R 再规划；模型自由直接学习价值或 Q。
5. 智能体刚进入新环境，无预知信息。
6. 可以，但有偏差。
7. 一次完整的从初始到终止的交互序列。
8. 通过试错更新价值。
9. 从的状态，动作，下一状态，奖励。
10. 遗憾 = 最优策略累积奖励 - 实际累积奖励。

### 5.2 Model-Based Learning
1. 计数 N(s,a,s')，然后归一化：N(s,a,s') / Σ_{s''} N(s,a,s'')。
2. 3/5, 2/5。
3. 记录的奖励平均值，或直接取观察值。
4. 用估计的 T̂,R̂ 运行值迭代或策略迭代。
5. 需存储计数，且模型可能不准确。
6. 归一化: (A,exit,x):1/1=1； (B,east,C):2/2=1（若只看到C）等。
7. 大数定律需要足够样本。
8. 可以用 ε-贪婪探索，同时更新模型。
9. 收敛到真实模型，故最优。
10. 模型基需维护计数和求解模型，模型自由更新简单但需多次采样。

### 5.3 Model-Free Learning
1. 维护每个状态的总收益和次数，相除得 V^π(s)。
2. 浪费转移信息，收敛慢。
3. V^π(s) ← (1-α)V^π(s) + α (R + γ V^π(s'))。
4. α 控制更新幅度，通常随时间递减。
5. 展开后历史样本权重指数衰减。
6. Q(s,a)←(1-α)Q(s,a)+α(R+γ max_a' Q(s',a'))；off-policy，因为不用当前策略选动作。
7. off-policy: 从不同策略（如随机）产生的数据学习最优策略。
8. Q(s,a) = w·f(s,a)，更新 w_i ← w_i + α·difference·f_i(s,a)。
9. 通过特征泛化到未访问状态。
10. 距离最近幽灵的距离、距离最近豆子的距离、是否处于危险等。

### 5.4 Exploration and Exploitation
1. 以 ε 概率随机动作，否则贪婪。ε=1 时完全随机。
2. 即使学到最优也会偶尔随机。改进：ε 衰减。
3. f(s,a) = Q(s,a) + k/N(s,a) 等等。
4. 鼓励少访问的 (s,a) 被选。
5. 自动降低 bonus，无需手动衰减。
6. 选择 argmax_a f(s,a)。
7. k 大则更探索。
8. 否则可能错过最优。
9. 乐观初始化将未访问 Q 设高，鼓励探索。
10. 遗憾界限为 O(√T) 在某些条件下。

## 第6章 Bayes Nets

### 6.1 Probability Rundown
1. P(A,B,C)=P(A)P(B|A)P(C|A,B)。
2. P(B|A)=P(A,B)/P(A)=0.1/0.2=0.5。
3. P(A|B)=P(B|A)P(A)/P(B)。
4. 给定 C 时 A,B 独立。
5. P(A|C)。
6. 将未归一化分布除以总和得到概率分布。
7. 例如 A 与 B 独立，B 与 C 独立，但 A 与 C 可通过 B 相关。
8. P(A,B)=Σ_c P(A,B,c)。
9. 归一化。
10. P(A∪B)=P(A)+P(B)。

### 6.2 Probability Inference
1. 计算给定证据下查询变量的后验。
2. 查询变量、证据变量、隐藏变量。
3. 选择与证据一致的行，对隐藏变量求和，归一化。
4. 2*2*2*3=24 行。
5. 可以，使用贝叶斯规则和边缘化。
6. 因为我们需要的是边缘分布，不需要隐藏变量值。
7. O(d^n)。
8. 计算联合后验 P(Q1,Q2|e)，同上。
9. 需要全联合表，指数大。
10. 内存和时间爆炸。

### 6.3 Bayesian Network Representation
1. DAG + CPT 表。
2. P(X1..Xn)=∏ P(Xi|Parents(Xi))。
3. 不一定，可以表示相关性但不一定是因果。
4. P(¬B)P(¬E)P(+A|¬B,¬E)P(+J|+A)P(¬M|+A)。
5. 利用条件独立性减少参数。
6. 父节点个数。
7. 2^n 个参数；若最多 k 个父节点，需 n·2^k 个参数。
8. 模型是真实世界的简化，总有不准确。
9. 无环避免无限依赖。
10. 图 X ← Y → Z，则 X ⟂ Z。

### 6.4 Structure of Bayes Nets
1. 给定父节点，节点与非后代独立。
2. 马尔可夫毯 = 父节点 + 子节点 + 子节点的其他父节点。
3. 使用拓扑排序和条件独立性消去祖先项。
4. 保证父节点在子节点之前。
5. {A, B, E}（A 为父，B,E 为子节点其他父？实际 J 的父是 A，所以马尔可夫毯包括 A）。
6. 不能直接看出绝对独立，需 d-分离。
7. 指数增长，2^|parents|。
8. Gibbs 采样时只用到局部条件。
9. d-分离。
10. 条件独立性可能不成立，可能全相关。

### 6.5 D-Separation
1. 图结构保证条件独立性。
2. 因果链：X⟂Z|Y；共同原因：X⟂Z|Y；共同效果：X⟂Z，但给定 Y 时不独立。
3. 不独立；给定 Y 独立。
4. 不独立；给定 Y 独立。
5. 独立；给定 Y 可能相关。
6. v-结构，碰撞节点；未观测时独立，观测后相关。
7. 若后代被观测，效果等同于观测 Y。
8. 列举路径，检查三元组活跃性（见算法）。
9. 整条路径活跃需所有三元组活跃。
10. 因为后代观测也会激活 v-结构。

### 6.6 Exact Inference in Bayes Nets
1. 变量消除逐变量乘因子再求和，避免构建全联合。
2. 因子是局部概率表（未归一化）。
3. 见教材图 14.11。
4. 最大因子的维度决定中间计算复杂度。
5. 先消 C 和 S，再处理 T。
6. 依赖于消元顺序，指数于树宽。
7. 因子相乘对应变量名交集，概率相乘但不必归一化。
8. 将所有涉及 X 的因子结合，再对 X 求和，消去 X。
9. 是，精确相同。
10. 可行且高效。

### 6.7 Approximate Inference in Bayes Nets: Sampling
1. 按拓扑顺序从 CPT 采样每个变量。
2. T: r=0.2<0.9 => true；C: r=0.7，给定 T 下 P(C|T)=0.8，0.7<0.8 => true。
3. 只保留与证据一致的样本。
4. 大量样本被拒绝。
5. 固定证据变量值，并给样本加权重 P(证据|样本)。
6. 权重 = 乘积 P(证据|父节点)。
7. 0.3。
8. 迭代采样每个变量给定其余变量（利用马尔可夫毯）。
9. 马尔可夫毯给出采样所需的最小条件概率。
10. 不丢弃样本，高效。

### 6.8 Summary
1. 紧凑表示，处理不确定性，结构化推理。
2. 变量少用精确推理；变量多用近似。
3. 枚举：概率值；变量消除：概率值；似然加权：加权样本统计；Gibbs：马尔可夫链样本统计。
4. n 大时。
5. 误差 ~ O(1/√N)。
6. 可能卡在局部模式。
7. 不同顺序产生不同大小的中间因子。
8. 最小度启发式，最小填充启发式。
9. 树宽为 1，线性。
10. 可以，任何分布可编码为 BN（但需全连通）。

## 第7章 Decision Networks and VPIs

### 7.1 Utilities
1. 最大期望效用原则。
2. A ≻ B 表示严格偏好 A。
3. 次序性、传递性、连续性、可替代性、单调性。
4. 彩票 = 概率分布 over 结果。
5. 0.5*100+0.5*0=50。
6. 风险中性：U(x)=x，风险厌恶：凹函数（如 √x），风险寻求：凸函数（如 x²）。
7. 期望效用 = 期望值。
8. 存在一个概率使得彩票与确定结果无差异。
9. 偏好满足公理则存在效用函数使决策等价于期望效用最大化。
10. 否则会出现循环偏好，被榨取。

### 7.2 Decision Networks
1. 机会节点（圆形）、行动节点（矩形）、效用节点（菱形）。
2. 与贝叶斯网络相同，有条件概率。
3. 智能体可选择其值。
4. 机会节点和行动节点。
5. EU(a|e)=Σ_s P(s|e,a) U(s,a)。
6. MEU(e)=max_a EU(a|e)。
7. a* = argmax_a EU(a|e)。
8. 机会节点：天气；行动节点：带伞/不带。
9. 结果树展开决策网络为 expectimax 树。
10. 差不多。

### 7.3 The Value of Perfect Information
1. VPI = 观测新证据后期望 MEU 的提升。
2. VPI(E'|e) = MEU(e,E') - MEU(e)。
3. 因为新信息不会降低最优决策质量。
4. 0。
5. 信息可能有交互作用，观测一个会改变对另一个的价值。
6. VPI(E1,E2|e) = VPI(E1|e)+VPI(E2|e,E1)。
7. MEU(E') = 0.59*95+0.41*53=77.78，VPI=7.78。
8. 如果 VPI > c 则观测。
9. 不可能为负，因为可以选择忽略观测，保持原 MEU。
10. 完美信息观测后确切知道变量值；部分信息观测结果可能仍有不确定性。

## 第8章 HMMs

### 8.1 Markov Models
1. 链状图 X0 → X1 → X2 → …
2. P(W0)∏ P(Wi|W_{i-1})。
3. 未来只依赖于现在。
4. 参数不随时间变化，简化。
5. P(W1)=Σ_{w0} P(W1|w0)P(w0)。
6. 0.4*0.8+0.9*0.2=0.32+0.18=0.5。
7. 分布 π 满足 π = π P（P 为转移矩阵）。
8. 解 π_s=0.6π_s+0.1π_r, π_s+π_r=1，得 π_s=0.2, π_r=0.8。
9. 初始分布本身。
10. O(n^2 t)（每步矩阵乘向量）。

### 8.2 Hidden Markov Models
1. 证据节点 Fi 对应每个 Wi。
2. P(W0)∏ P(Wi|W_{i-1})∏ P(Fi|Wi)。
3. P(Fi|Wi)，通常假设平稳。
4. B(Wi) = P(Wi|f1..fi)。
5. 时间：B'(W_{i+1})=Σ_{wi} P(W_{i+1}|wi) B(wi)；观测：B(W_{i+1}) ∝ P(f_{i+1}|W_{i+1}) B'(W_{i+1})。
6. 时间更新得 B' = [0.5,0.5]，观测得未归一化 [0.4,0.15]，归一化 [0.727,0.273]。
7. 避免每次计算归一化常数，最后一步归一化。
8. O(n^2) 每步（n 状态）。
9. 只需保留当前信念，不需所有历史。
10. 标准马尔可夫模型。

### 8.3 Viterbi Algorithm
1. 求最可能隐藏状态序列 argmax_{x1:T} P(x1:T|e1:T)。
2. m_{t}(j) = max_i [m_{t-1}(i) P(Xt=j|X_{t-1}=i) P(et|Xt=j)]。
3. 子问题最优性。
4. 每个状态的最优前驱指针。
5. 用动态规划，从 t=1 到 T 递推，最后回溯。
6. 同为 O(T n^2)。
7. 因为解码出隐藏状态序列。
8. 模型可能差或观测噪声大。
9. 使用对数概率。
10. 是，动态规划保证全局最优。

### 8.4 Particle Filtering
1. 用一组粒子（样本）近似信念分布。
2. 一个隐藏状态值的样本。
3. 对每个粒子，根据转移模型采样新状态。
4. 权重 = 传感器概率 P(观测|状态)。
5. 根据权重重采样粒子（带放回）。
6. 随着粒子数大，近似真实分布。
7. 估计不准确，可能漏掉真实状态。
8. O(M·状态转移时间)。
9. 可处理大状态空间，复杂度不指数于状态数。
10. 都通过证据加权。

### 8.5 Summary
1. 马尔可夫模型预测状态演化；隐马尔可夫模型加入观测。
2. B_t ∝ P(e_t|X_t) Σ_{x_{t-1}} P(X_t|x_{t-1}) B_{t-1}。
3. 同上但用 max 替代 sum。
4. 时间更新、加权、重采样。
5. 粒子滤波。
6. 非周期不可约才存在唯一平稳分布。
7. 样本数增加，误差减小。
8. 信念朝更可能对应观测的状态集中。
9. Viterbi 找单条最可能路径；forward 给出各位置概率分布。
10. 略。

## 第9章 ML

### 9.1 Machine Learning
1. 输入-输出对，学习映射（如分类）。
2. 只有输入，发现模式（如聚类）。
3. 训练：学习参数；验证：调超参数；测试：评估泛化。
4. 模型设计参数（如学习率、k），通过验证集选择。
5. 避免过拟合到测试集。
6. 模型过度拟合训练数据，泛化差。
7. 从原始数据提取有用特征。
8. 训练 = 学习参数；推理 = 对新数据预测。
9. 验证集用于调参，测试集最终评估。
10. 过拟合或验证集分布与测试集不同。

### 9.2 Naive Bayes
1. 特征在给定类别下条件独立。
2. ŷ = argmax_y P(y) ∏ P(f_i|y)。
3. 因为特征独立的假设往往不成立。
4. 条件独立于类别，绝对不独立。
5. P(spam|f) ∝ 0.3Π P(f_i|spam)；选概率大的。
6. θ̂ = (正例数)/(总例数)。
7. 加 k 伪计数避免零概率。
8. (count(1,y)+1)/(count(y)+2)。
9. 先验：1 个参数（类别先验）；条件：每特征每类 1 个参数（二值），共 2n 个。
10. 可以假设高斯分布。

### 9.3 Perceptron
1. activation = w·f(x)。
2. 超平面（线性边界）。
3. w ← w + y* f(x)（当预测≠y*）。
4. w ← w + (+1)·f(x)。
5. 数据线性可分且使用足够迭代。
6. 添加常数特征 1，对应偏置权重。
7. 不能，XOR 线性不可分。
8. 对正确类加 f，对预测类减 f。
9. 是，感知机收敛定理。
10. 旋转权重向量使之与误分类样本夹角减小。

### 9.4 Binary Perceptron
（延续上一节，略重复，补充）
1. 0.5*1 + (-1)*2 = 0.5-2=-1.5 <0 => 负类。
2. w ← [0.5,-1] + [1,2] = [1.5,1]。
3. 视数据规模，几次到几十次。
4. 不收敛，权重震荡。
5. 感知机不输出概率，逻辑回归输出概率。
6. 符号函数输出 ±1，sigmoid 输出概率。
7. 可增量更新，适合流数据。
8. 多类感知机（每个类一个权重向量）。
9. 感知机是 SVM 的前身，SVM 最大化间隔。
10. 合页损失 max(0, -y*activation)。

### 9.5 Linear Regression
1. 连续值。
2. h_w(x)=w0 + w1 x1 + ... wn xn。
3. 均方误差：½ Σ (y-h)^2。
4. ŵ = (X^T X)^{-1} X^T y。
5. 特征共线性或样本少于特征数；加正则项或伪逆。
6. w ← w - α (X^T X w - X^T y)。
7. 避免过拟合。
8. 验证集误差上升而训练集下降。
9. 梯度和数值更稳定。
10. 解方程：2 = w0 + w1, 3 = w0+2w1 → w1=1, w0=1。

### 9.6 Optimization
1. 上升增函数，下降减函数。
2. w ← w - α ∇ Loss(w)。
3. 太大不收敛，太小收敛慢。
4. 每次用一个随机样本估计梯度。
5. Batch 稳定但慢；SGD 快但随机。
6. 小批量平衡效率和稳定性。
7. 凸函数任何局部最优即全局。
8. 随时间减小学习率。
9. 梯度模很小，或损失变化很小。
10. 当特征维度大时，闭式解 O(n^3) 太贵。

### 9.7 Logistic Regression
1. 二分类；输出 (0,1) 概率。
2. σ(z)=1/(1+e^{-z})。
3. P(y=1|x)=σ(w^T f(x))。
4. L = ∏ σ(w^T f(x_i))^{y_i} (1-σ)^{1-y_i}。
5. 交叉熵损失，而非平方损失。
6. 平方损失非凸且梯度不好。
7. ∂Loss/∂w_i = (σ - y) x_i。
8. 线性（超平面）。
9. softmax。
10. 对，伯努利。

### 9.8 Multi-Class Logistic Regression
1. softmax(z)_j = e^{z_j} / Σ_k e^{z_k}。
2. P(y=2|f) = exp(w2·f) / (exp(w1·f)+exp(w2·f)+exp(w3·f))。
3. K×d。
4. Σ_i Σ_k t_{i,k} log(softmax_k)。
5. 指数为正，分母归一化。
6. ∇_{w_j} log L = Σ_i (t_{i,j} - softmax_j) f(x_i)。
7. w_j ← w_j + α ∇_{w_j} log L。
8. 一对多训练 K 个二分类器；多类 softmax 一次参数共享。
9. 不变（因为分母平移抵消）。
10. 对权重的 ℓ2 正则化。

### 9.9 Neural Networks: Motivation
1. 单层只能线性边界。
2. 特征映射（如 x²）使数据在高维线性可分。
3. 多层可拟合任意函数。
4. 足够多神经元的双层网络可逼近任意连续函数。
5. 否则多层仍是线性。
6. sigmoid, tanh, ReLU。
7. ReLU(x)=max(0,x)。
8. 梯度消失：导数值小，深层梯度消失；ReLU 缓解。
9. 一组神经元，输入到输出映射。
10. 反向传播：计算损失对每层权重的偏导，链式法则。

## 第10章 Logic

### 10.1 A Knowledge Based Agent
1. 知识库（KB）。
2. 一组逻辑句子。
3. RL 基于奖励试错，KB 基于推理。
4. 从已知句子推导新句子。
5. 若 KB 包含“如果热则前方有危险”且传感器热，则推理出危险。
6. 事实（原子命题）和规则（蕴含式）。
7. Tell 增加知识，Ask 查询是否蕴含。
8. Wet → Rain（或雨天导致地湿）。
9. 对符号的真值指派。
10. 将领域知识转化为逻辑形式。

### 10.2 The Language of Logic
1. 语法：合法符号串；语义：符号在模型中的真值意义。
2. ¬, ∧, ∨, ⇒, ⇔。
3. 仅当两者真时为真。
4. 仅当前真后假时为假。
5. 命题或其否定，如 P 或 ¬P。
6. 合取：∧；析取：∨。
7. (P⇒Q)∧(Q⇒P)。
8. P∨Q 真；P⇒Q 假（因为真⇒假）。
9. ¬(P∧Q) ≡ ¬P ∨ ¬Q。
10. Rain ⇒ Wet。

### 10.3 Propositional Logic
1. 代表真假的符号（如 P）。
2. 给命题符号赋值。
3. 2^n。
4. 有效：全真；可满足：至少一真；不可满足：全假。
5. 子句（析取）的合取。
6. ¬A ∧ ¬B。
7. (¬A∨B)∧(A∨¬B)。
8. 真值表显示永真。
9. 永真式。
10. 不可满足（KB ∧ ¬q 无模型）。

### 10.4 Propositional Logical Inference
1. 所有使 KB 为真的模型也使 q 为真。
2. 直接证明（KB⇒q 有效）或反证法（KB∧¬q 不可满足）。
3. 枚举所有模型；O(2^n)。
4. co-NP-complete。
5. 判断 CNF 的可满足性。
6. 早期终止、纯符号、单元子句。
7. 若某子句在部分赋值下已真，则不必继续。
8. 符号只以同一种形式出现。
9. 子句只有一个文字。
10. 不可满足。

### 10.5 Theorem Proving
1. (P ∨ Q) ∧ (¬P ∨ R) ⇒ (Q ∨ R)。
2. 将 KB 和 ¬q 转为 CNF，反复归结，若推出空则 KB⊨q。
3. 霍恩子句（至多一个正文字）。
4. 已知事实队列。
5. 已知 A，使用 A⇒B 得 B，再用 B⇒C 得 C。
6. 从目标开始反向推理。
7. 最多一个正文字的子句，如 ¬P ∨ ¬Q ∨ R。
8. 对霍恩子句集，前向链接完备。
9. 对一阶逻辑也完备。
10. ¬P ∨ R。

### 10.6 Forward Chaining
1. 记录每个子句前提中未满足的符号数。
2. 已知为真的符号。
3. [A, C]。
4. 4 次（A→B,B→? 教材示例）。
5. 是，因为有 inferred 表防止重复。
6. 线性于 KB 大小。
7. 不能，因为霍恩子句不允许否定结论？实际上可以，只是限定形式。
8. 对霍恩子句完备。
9. 所有可达结论推出后算法终止，返回 false。
10. 避免重复推导同一事实。

### 10.7 First Order Logic
1. 对象、谓词、函数、量词。
2. 常量、变量、函数应用（如 f(a)）。
3. 关系，如 Brother(x,y)。
4. ∀：所有；∃：存在。
5. ∀x Person(x) → Mortal(x)。
6. ∃x Person(x) ∧ Likes(x, Chocolate)。
7. ¬∀x P ≡ ∃x ¬P；¬∃x P ≡ ∀x ¬P。
8. 函数返回对象，谓词返回真值。
9. 谓词加上项，如 Mother(John, Mary)。
10. Father(x)=y 使用等词，或 Father(x,y) 谓词。

### 10.8 First Order Logical Inference
1. 将 FOL 转化为命题逻辑，用命题推理。
2. 若 FOL 句子不可满足，则存在有限命题化不可满足。
3. 若存在置换 θ 使前提合一，则可推出结论的实例。
4. 找到使两个表达式相同的变量替换。
5. 合一 θ={x/John}，得出 Happy(John)。
6. 维护事实库，每次应用规则添加新事实。
7. 用 Skolem 常数/函数消除存在量词。
8. 转换为 CNF，进行合一和归结。
9. 不存在判定算法，但半可靠。
10. 将命题规则提升到一阶，合一代替真值匹配。

### 10.9 Logical Agents
1. 随时间变化的性质，如 Hot(t)。
2. F^{t+1} ⇔ (ActionCausesF^t) ∨ (F^t ∧ ¬ActionCausesNotF^t)。
3. 用 SAT 求解器找到动作序列。
4. 将规划问题编码为 SAT，找模型。
5. 效果公理：动作导致的效果；框架公理：保持不变的属性。
6. 解决框架问题，仅需指定变化。
7. 区分不同时刻的状态。
8. 使用并发公理或规划器处理。
9. 用一阶逻辑滤波（如使用归结）。
10. 逻辑推理需要完整模型，RL 不需要，但逻辑更准确。

### 10.10 Summary
1. 命题逻辑用布尔变量，一阶逻辑用对象、谓词、量词。
2. CNF 可满足性。
3. 霍恩子句集。
4. 使用合一处理变量。
5. (α ∨ β), (¬β ∨ γ) ⊢ (α ∨ γ)。
6. 使用时间参数。
7. 将领域知识表示为逻辑形式。
8. 更自然表达，但推理复杂度高。
9. “每个学生都有一个导师”需要量词。
10. 逻辑是知识表示与推理的基础，与概率方法互补。