## 简述深度优先搜索（DFS）和广度优先搜索（BFS）在搜索树中的主要区别，并说明各自适用的场景。
- DFS的特点

深度优先搜索比广度优先搜索使用的内存更少。

- BFS的特点


## 考虑一个3×3的网格世界，从左上角(1,1)到右下角(3,3)，每次可向右或向下移动一格（无障碍）。画出BFS和DFS（按右、下顺序）找到的路径，并说明BFS为什么能找到最短路径。

## 用A*算法在以下图中找到从S到G的最短路径。
- 启发式h如下：S:5, A:3, B:4, C:2, D:3, G:0。
- 边权：S-A=2, S-B=1, A-C=2, A-D=1, B-D=2, C-G=2, D-G=3。
- 写出A*扩展的节点顺序及f值。


## 证明或反驳：
对于同一问题，如果启发式函数 h_2 (n)≥h_1 (n) 且两者都可采纳，则使用 h_2 的A扩展的节点数一定不多于使用 h_1 的A。请阐述你的推理。

## 设计一个8数码问题的可采纳启发式函数（不限于曼哈顿距离），并解释为什么它是可采纳的。给出一个初始状态（如目标状态为1 2 3 / 4 5 6 / 7 8 空格），用你设计的启发式函数计算其h值，并与曼哈顿距离比较。