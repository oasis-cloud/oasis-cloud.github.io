## 简述极小化极大算法的基本思想。为什么在完全信息零和博弈中它能找到最优策略？

## 给定一个博弈树（MAX根，深度2，所有叶节点值如下）：左分支（MIN节点）下叶值[3, 5]；右分支（MIN节点）下叶值[2, 9]。

- 请画出该树并标出每个节点的极小化极大值。
- 使用Alpha-Beta剪枝（从左到右遍历），写出哪些叶节点被剪枝，并说明根节点的最终值。

## 说明“地平线效应”，并解释为什么静止搜索可以缓解它。

## Alpha-Beta剪枝的效率与移动顺序高度相关。对于题目2中的右子MIN节点，如果将子节点顺序交换（先9后2），剪枝过程会发生什么变化？请解释为什么最佳移动优先策略能大幅提升剪枝效果。

## 设计一个五子棋的评估函数（不需编程，描述特征和权重设计思路）。说明如何避免因权重设置不合理导致AI走出“愚蠢”的棋步。

## 简述蒙特卡洛树搜索的四个步骤，并指出它与极小化极大算法的两个主要区别（从适用范围、评估方式等角度）。

## 国际象棋的Alpha-Beta引擎通常使用深度截断+评估函数。假如你被要求为某新棋类游戏（分支因子≈35，平均对局长度80步）设计AI，但无法写出精确评估函数（只能判断胜负）。你应该选择Alpha-Beta还是MCTS？为什么？请阐述你的理由。

## 在Alpha-Beta中引入“置换表”（transposition table）可以存储已评估过的局面，避免重复搜索。请说明置换表如何与α/β值配合工作（即存储什么信息，如何用于剪枝）。

## 假设你正在编写一个双人零和博弈AI，已知对手并非完全理性（时而随机走步）。此时使用标准极小化极大算法会有何问题？你会如何修改评估函数或搜索策略来应对？

## 查阅资料了解AlphaGo Zero的自我对弈训练机制。它如何结合MCTS与深度神经网络？其中神经网络扮演了哪些角色（策略网络、价值网络）？请简述。

