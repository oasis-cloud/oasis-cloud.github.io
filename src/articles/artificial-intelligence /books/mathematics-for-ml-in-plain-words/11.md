---
title: "白话机器学习的数学"
subTitle: ""
author: oasis
date: 2026-06-08
tags: [AI]
---

机器学习的两个方向：回归和分类。

- 回归：例如根据历史数据预测未来广告点击率。
- 分类：分类等同于标签，例如学生在某个分数线之上打成绩良好、此分数线之下打成绩一般。分类是对数据特征的学习，需要使用向量进行计算。

机器学习的工程实践步骤如下：
1. 需求与问题定义：明确业务目标、判定任务类型（分类 / 回归 / 聚类等）
2. 数据收集：获取原始数据集
3. 数据预处理：清洗、降噪、特征工程、数据集划分（训练 / 验证 / 测试集）
4. 模型选型与建模：挑选适配算法、搭建模型结构
5. 模型训练：输入训练集，借助梯度下降等优化算法迭代更新参数、拟合数据
6. 模型评估与验证：用验证 / 测试集检验效果，查看指标、排查过拟合 / 欠拟合
7. 模型调优：调参、优化特征、更换模型，反复迭代
8. 部署与监控：模型上线，线上持续监控效果

## 前置数学知识
### 微分和偏微分
#### 微分
- 规则1：幂函数。函数：$f(x) = x^n$ 
- 规则2：常数倍。函数 $f(x)= nx$
- 规则3：和与差 函数
- 规则4：乘积法则 函数
- 规则5：链式法则 函数

### 向量

## 回归
本书通过广告投放和点击量的例子讲解回归:

1. 根据问题设计模型函数: \ftheta(y) = theta + theta*x\
2. 根据模型函数设计目标函数：
\[
E(\theta) = \frac{1}{2} \sum_{i=1}^n \left( y^{(i)} - f_\theta(x^{(i)}) \right)^2
\]
目标函数的思想是：数据中的广告点击量和预测得出的广告点击量，计算方差，并且要把所有数据的方法累加在乘0.5。
3. 求解目标函数的方式
   - 最小二乘法（梯度下降）：$x := x - \eta \frac{d}{dx} g(x)$ ，其中 $\eta$ 是表示学习率。此值的选择需要根据数据进行尝试。太大下降速度快、可能出现循环
   - 多项式回归：将 $f\theta(y) = theta + theta*x$ 改为 $f\theta(y) = \theta + \theta x + \theta x^2$。因为之前的函数是一次函数，表示一条直线，实际的数据分布情况用曲线能更好的拟合。
   - 多重回归：利用多参数分别进行回归
   - 随机梯度下降：
4. 

