## 考虑一个教室排课问题：有3门课程（C1,C2,C3），2个教室（R1,R2），5个时间段（T1..T5）。课程C1和C2有共同学生（不能同时同地），C2和C3有共同教师（不能同时不同地）。请将该问题形式化为CSP（明确变量、值域、约束类型）。

## 下图是一个简单地图着色问题（四个区域A,B,C,D，边表示相邻），颜色集{红,绿,蓝}。
- 画出约束图。 
- 使用回溯搜索（按A,B,C,D顺序），手工找出一个合法解，并说明在第几次赋值时发生回溯（如有）。

## 给定四个变量X,Y,Z,W，值域均为{1,2,3}。约束：X=Y，X≠Z，Y>W，Z+W=4。
- 画出约束图。
- 手动执行AC-3算法，写出每一步弧检查后各变量的值域变化。

## 解释“最少剩余值（MRV）”和“度启发式（Degree Heuristic）”在回溯搜索中的作用。举例说明MRV如何减少搜索节点数。

## 使用最小冲突法求解4皇后问题。
- 描述算法步骤。
- 若初始随机放置为：第1列皇后在第1行，第2列第2行，第3列第3行，第4列第4行，计算初始冲突数，并演示两次迭代（每次选择一个冲突最大的皇后移动到冲突最小的位置）。

## 某大学有6门考试（E1..E6），学生选课数据给出冲突图如下：E1与E2,E3冲突；E2与E4冲突；E3与E4,E5冲突；E4与E6冲突；E5与E6冲突；E1与E6不直接冲突。每个考试安排一天，问最少需要几天？
- 将此问题建模为CSP（变量、值域、约束）。
- 利用约束图分析该图的色数（clique number、贪心着色）。
- 如有必要，设计一个启发式顺序并求解。

## 已知一个CSP的约束图是一个树（无环），节点为A,B,C,D,E，边为A-B, B-C, C-D, D-E。所有变量值域大小为k，二元约束。
- 简述如何在O(nk²)时间内求解（写出算法思路：选择根，前向传播，后向赋值）。
- 若这个树有一个额外边B-D，图变成单环，你如何利用树分解技巧求解？

