---
description: 向 docs/index.md 添加/更新文章目录索引的规范与操作步骤
globs: docs/index.md
alwaysApply: false
---

# 目录索引规范（index.md）

新建或发布文章后，必须在 `docs/index.md` 登记一条目录项。本文件是站点首页目录。

## 条目格式

```markdown
* [显示标题](/articles/<相对路径>.html)
```

- 源文件 `docs/articles/foo/bar.md` → 链接 `/articles/foo/bar.html`（`.md` 换成 `.html`，保留目录结构）。
- 列表标记用 `*`（与现有目录一致），不要改成 `-`。
- 站内文用根路径 `/articles/...`；外链才用完整 `https://...`。
- 添加前先搜索：同一路径不得重复出现。

## 显示标题怎么写

优先可读的目录名，不必逐字复制 frontmatter：

| 类型 | 写法 | 示例 |
| --- | --- | --- |
| 系列分章 | `系列名（序号）` 或 `系列名：章节主题` | `从零构建大语言模型（一）`、`MCP 入门：基础概念` |
| 单篇读书笔记 | 常用书名/短标题 | `程序是怎么跑起来的` |
| 技术单篇 | 简明描述主题 | `DNS 解析流程` |

系列内序号风格与同组已有条目保持一致（中文「一、二」或阿拉伯数字二选一，勿混用）。

## 放到哪一组

1. 先找是否已有同系列/同目录分组（路径前缀相同，如 `books/how-program-works/`）。
2. 有则插在该组末尾（或按章节序号排序），不要新开一组。
3. 无则并入主题相近的分组；仍无合适分组时，在主题区域末尾新建一组。
4. **分组之间**用单独一行的 `<br />` 分隔，且**上下各留一空行**（否则会被 Markdown 并进上一条列表项，看不到分组间距）；组内条目连续排列，中间不加 `<br />`。

常见主题区域（自上而下参考）：`books/how-program-works` → AI（`ai/mcp`、`ai/vibe-coding`）→ AI 书籍（`ai/books/`）→ 前端源码（`frontend/source-code`、`immerjs`）→ Next/React（`frontend/nextjs`、`react`、`zustand`）→ Taro/NutUI → 其它前端（`frontend/javascript`、`typescript`、`css`、`browser`、`tools`、`micro-frontends`）→ Node（`nodejs/`）→ GitHub（`github/`）→ 网络（`network/`）→ 软件设计（`software-design/`）→ 计算机经典（`books/sicp`、`philosophy-of-software-design`、`programming-style`）→ 通识读书笔记（`books/`）→ 随笔（`essays/`）。

目录组织详见 `.cursor/rules/articles-structure.mdc`。

## 操作检查清单

新增文章并登记索引时：

1. 确认文章已落在 `docs/articles/.../*.md`，路径稳定。
2. 在 `docs/index.md` 写入一条正确的 `* [标题](/articles/...)`。
3. 核对分组、序号、无重复链接。
4. 不要改动文件顶部 frontmatter（`title` / `subTitle`），除非用户明确要求。
