---
description: 博客文章 Markdown 排版规范（frontmatter、标题、数学公式、列表、图片与链接）
globs: src/articles/**/*.md
alwaysApply: false
---

# 文章排版规范

编写或修改 `src/articles/**/*.md` 时遵循本规范。站点使用 Graphite + pymdownx.arithmatex（KaTeX）。

## Frontmatter

```yaml
---
title: 书名或系列主标题
subTitle: 本章/本节副标题   # 可选；系列文章建议写
author: oasis
date: YYYY-MM-DD
tags: [读书笔记]           # 或 [AI]、[llm]、[MCP]、[AI, Math] 等
---
```

- `author` 固定为 `oasis`；`date` 用 `YYYY-MM-DD`。
- 含冒号、引号的标题用双引号包裹。
- 系列读书笔记：`title` 为书名，`subTitle` 为章节主题。

## 标题与结构

- 正文从 `##` 开始；小节用 `###`；避免跳级。
- 一篇一文，按主题分节；每节一个核心点。
- 中文叙述为主；专有名词首次出现可写「中文（English）」。

## 数学公式（arithmatex / KaTeX）

- 行内：`$n$`、`$2^{-1}$`、`$\mathrm{bias}$`
- 独立公式独占一行，上下空行：

```markdown
$$
a^{-n} = \frac{1}{a^{n}}
$$
```

- 多字母标识用 `\mathrm{...}`；二进制可按 4 位分组：`$0000\ 0001$`
- 不要用裸文本写分数/上下标；不要混用 `\(...\)` / `\[...\]`（源码统一 `$` / `$$`）。

## 列表、表格、强调

- 无序列表用 `-`；有序步骤用 `1.`。
- 关键点加粗：`- **精确订阅**：只有…`
- 对照信息用 Markdown 表格；表头与分隔行齐全。

## 代码与图片

- 代码块必须标明语言：` ```js `、` ```ts `、` ```python `
- 图片：`![简短说明](/images/<系列或主题>/xxx.png)`，路径相对站点根。
- 文内互链优先：`[标题](@root/articles/.../1.html)`

## 文风

- 短段、直说；先结论或定义，再展开例子。
- 不堆砌 emoji；不滥用零宽字符/异常加粗装饰。
- 修改旧文时向本规范靠拢，不借机大段重写无关内容。
